# Accord — v1 Build Specification

> **Status:** specified (not yet built). The authoritative rationale lives in
> `apps/docs/docs/adr/0003`–`0012`; the domain language in `CONTEXT.md`. This file is the
> implementation reference: account model, instructions, state machine, economics.
> Code is authority on current state; this spec is authority on intent.

## Overview

`programs/accord` is a general-purpose, Schelling-point arbitration primitive on
Solana. It is **party-agnostic**: any Solana program (the _Arbitrable_) files a
question via CPI; the Accord draws stake-weighted Jurors, collects commit-reveal
votes, and writes a Ruling the filer reads lazily. It ships first as a standalone product.

Mechanism and economics are inherited from Kleros (live since 2019, 1000+ disputes);
the Solana-specific deviations are the draw (ADR-0012 — on-chain stake accumulator,
superseding ADR-0003/0008/0009's optimistic snapshot), the party model (ADR-0004),
Subaccord authority (ADR-0005), evidence (ADR-0006), and upgrade (ADR-0007).

## Account / PDA model

| Account         | Seeds                                     | Key fields                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------------- | ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Subaccord`     | `["subaccord", creator, domain_ref_hash]` | `staking_token` (collateral mint, ADR-0002/0020), `fee_token` (compensation mint, ADR-0020), `min_stake`, `alpha_bps` (slash = `alpha_bps * min_stake / 10_000`), `review/commit/reveal_window`, `appeal_window` (per-Subaccord, ADR-0022; default `DEFAULT_APPEAL_WINDOW_SECS` = 3 days), `max_appeals` (bounds the appeal ladder depth), `min_jury_size` (round-1 panel size, default 3, accord-9q3e; must be odd; ladder top `(J+1)·2^max_appeals − 1 ≤ MAX_JURORS`; immutable at creation), `aggregation: Aggregation` (dispute-kit tally rule, ADR-0019: `Plurality` = option-index votes, `Median` = scalar u64 votes, ADR-0025), `fee_per_juror` (in `fee_token`), `reveal_threshold_bps: u16` (reveal-quorum fraction, default 6_666 = 2/3, ADR-0021), `shortfall_policy: ShortfallPolicy` (v1 = `Redraw`, ADR-0021), `max_draw_attempts: u8` (same-size redraw cap per round, default 3, ADR-0021), `coherence_tol_bps: u16` (Median coherence band in bps of the final median, default 100 = ±1%; `0` = exact; validated `≤ 10_000`; inert for `Plurality`; immutable — not in `UpdatePayload`; ADR-0025), `authority: Pubkey` (`default` ⇒ immutable), `evidence_operator: Pubkey`. `juror_credential`/`juror_schema: Pubkey` (attestation gate — default ⇒ stake-only; PROG-ATTESTTION/ADR-0024). `domain_ref` + `evidence_spec` + `juror_credential` + `juror_schema` immutable (identity triplet + credential binding). **Accumulator:** `root_hash: [u8;32]`, `total_stake: u64`, `next_index: u32`, `depth: u8` (fixed at creation, default 20 ≈ 1M seats). |
| `JurorStake`    | `["stake", subaccord, juror]`             | `staked: u64` (collateral), `fees_earned: u64` (compensation aggregate, ADR-0020), `active_draws: u32`, `tree_index: u32` (assigned at first stake, immutable), `stake_delta: i64` (pending slash/reward), `slash_reserve: u64`, `pending_withdrawal: u64`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `Dispute`       | `["dispute", filer, nonce]`               | `subaccord`, `options: [..; MAX]` (empty for `Median` scalar disputes, ADR-0025), `evidence_hashes: [[u8;32]; NUM_EVIDENCE_SLOTS]` (per-round; `[0]`=filing, appeal rounds optional, `[0u8;32]` sentinel=reuse prior, milestone accord-qp7c), `state`, `current_round`, `terms: CaseTerms` (filing-time freeze of economics + `aggregation` + `coherence_tol_bps` + `appeal_window` + `min_jury_size`, ADR-0019/0022/0025/accord-9q3e), `final_ruling: u64` (`u64::MAX` until `Final`; the winning option index for `Plurality`, the final median for `Median`, ADR-0025), `fee_paid: u64` (running available fee pool, ADR-0020), `committed_vrf: Option<[u8;32]>`, `frozen_root: [u8;32]` (set in `commit_vrf_callback`), `frozen_total_stake: u64`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `Round`         | `["round", dispute, round_idx]`           | `#[zero_copy, repr(C)]`, fields grouped by width for `bytemuck::Pod` (ADR-0025 re-layout): u32 block (`round_idx`, `juror_count`, `commit_count`, `reveal_count`, `draw_attempt: u32` (same-size redraw counter, orthogonal to `round_idx`; ADR-0021)) → u8 scalars (`settled`, `bump`) + 2-byte pad fill the alignment hole → i64 windows (`review_end`, `commit_end`, `reveal_end`) + `result: u64` (`u64::MAX` = not set; option index / median, ADR-0025) → `dispute: Pubkey`, `jurors: [Pubkey; MAX_JURORS]`, `commits: [[u8;32]; MAX_JURORS]` (`hash(vote_le8 ‖ salt ‖ juror)` per juror) → u64 arrays `seat_prefix`, `seat_stake`, `reveals: [u64; MAX_JURORS]` (`u64::MAX` until revealed — option index for `Plurality`, fixed-point scalar for `Median`; struct offset 2576)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `AppealBond`    | `["bond", dispute, round_idx]`            | `dispute`, `round_idx` (the round the appeal opens — the larger panel), `appellant`, `amount: u64` (**total** deposit = appeal fee + bond; only the bond is refundable, bean accord-xftx), `prior_result: u64` (winning value of the round being appealed — option index for `Plurality`, median for `Median`; flip check at settlement = `final_ruling != prior_result`; u64 since ADR-0025), `bump`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `PendingUpdate` | `["update", subaccord, nonce]`            | proposed field+value, `execute_after_slot` (48h timelock)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `stake_vault`   | Subaccord-PDA ATA of `staking_token`      | juror collateral (ADR-0020). Touched only by `stake`/`withdraw` + slash ledger.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `fee_vault`     | Subaccord-PDA ATA of `fee_token`          | filer fees + appeal bonds + reward pool (ADR-0020). Touched by `create_dispute`/`appeal`/`withdraw_fees`/`cancel_dispute`/`claim_appeal_refund`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

The `Snapshot` account, its bond, its 1-day challenge window, and the four fraud
predicates are **deleted** (ADR-0012): the juror-set root is canonical by
construction, maintained live by `stake`/`unstake`, so there is nothing to post,
challenge, or finalize.

`MAX` option count and `MAX_JURORS` (= 31, the 3rd-appeal panel) are compile-time
constants bounding account size.

`Subaccord`, `JurorStake`, `Dispute`, `AppealBond`, and `AccordState` each carry a
trailing `padding: [u8; 64]` — zeroed reserved tail space (always the last field) so
future fields can be carved out without moving existing offsets or resizing live
accounts. `Round` (zero-copy, byte-exact `Pod` layout) and `PendingUpdate` carry none.

## Instructions

| #   | Instruction                                                                                                      | Semantics                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `create_subaccord(params, authority, evidence_operator)`                                                         | permissionless; inits `Subaccord` (incl. empty accumulator: `root_hash = H(empty)`, `next_index = 0`, `depth` fixed at creation, default 20 ≈ 1M seats). `params.min_jury_size` sets the round-1 panel (default 3, accord-9q3e; odd, ladder must fit `MAX_JURORS`). `params.juror_credential`/`juror_schema` bind an optional SAS attestation gate (PROG-ATTESTTION/ADR-0024): **both-or-neither** — a half-bound Subaccord reverts (`AttestationBindingPartial`); both `default` ⇒ stake-only (today's behavior). Immutable once set.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2   | `propose_subaccord_update` / `execute_subaccord_update`                                                          | authority-signed; on-chain 48h timelock. No-op if `authority == default`. Stakers can unstake before execution.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 3   | `stake(amount, leaf_path)` / `request_withdraw(amount, leaf_path)` / `withdraw()` / `reconcile_stake(leaf_path)` | SPL transfer into/out of `stake_vault` (ADR-0020); caller supplies the juror's Merkle path, the chain verifies it vs the stored root, reads the **live** `JurorStake.staked`, applies the verified vault delta, and recomputes the root. O(log N). First stake appends a leaf and assigns `tree_index` (or pops a recycled slot from the free list if one is available — RECLAIM-LEAF); full unstake zeros the leaf (kept at zero selection weight). `unstake` reverts while `active_draws > 0`. **Gated pools (PROG-ATTESTTION):** `stake` takes the juror's SAS attestation in `remaining_accounts[0]`; its credential/schema must match the Subaccord binding, its subject wallet must be the juror, and (unless `expiry == 0` ⇒ never expires) it must outlive the max dispute lifecycle horizon (`AttestationExpired`). Stake-only Subaccords skip this. When a gated pool stakes into a recycled slot the attestation stays at `remaining_accounts[0]` and the freed slot follows at `[1]` (gated-aware index). |
| 4   | `create_dispute(subaccord, options, evidence_hash, fee)`                                                         | **[Arbitrable CPI]** filer pays full fee; a separate data-free `rent_payer` signer carries dispute-init + fee_vault rent (PDA filers cannot — the system program rejects transfers from data-carrying accounts; Arbitrables pass their crank caller). Reverts if active distinct stakers < `min_jury_size` (round-1 panel; accord-9q3e). Filer fee = `min_jury_size · fee_per_juror`. **Options gate (ADR-0025):** `Plurality` disputes enumerate `2..=MAX_OPTIONS` (8) option hashes; `Median` (scalar) disputes file with **zero** options — the vote is a u64 fixed-point value, not an index. Does **not** freeze the root (capital stays live).                                                                                                                                                                                                                                                                                                                                                                  |
| 5   | `request_vrf` / `commit_vrf_callback(randomness)`                                                                | `request_vrf` CPIs the VRF oracle; the oracle's identity-constrained callback writes `committed_vrf` **and** freezes `dispute.frozen_root = subaccord.root`. One VRF + one frozen root serve the whole dispute (all appeal rounds draw a larger panel from the same fixed pool).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 6   | `draw_seat(dispute, subaccord, seat_index, juror_membership)`                                                    | consumes the **VRF**; **one seat per tx** (the 1232-byte packet cannot hold N proofs). Verifies MST membership + sortition (`prefix ≤ r_i < prefix + stake`, prefix from authenticated sibling sums) against `frozen_root`; inflation guard (`JurorStake.staked ≥ leaf.stake`); `active_draws += 1`. The drawn juror's `JurorStake` is `remaining_accounts[0]`. **Gated pools (PROG-ATTESTTION):** the `subaccord` account is now passed (read-only, constraint-bound to the dispute), and the juror's SAS attestation rides in `remaining_accounts[1]` for a defense-in-depth freshness re-check (`expiry == 0` or `expiry > now`). N txs make a panel; deterministic sampling without replacement (no `draw_attempt` grind).                                                                                                                                                                                                                                                                                        |
| 7   | `commit(dispute, h)`                                                                                             | `h = hash(vote_le8 ‖ salt ‖ juror_pubkey)` — the vote hashed as an **8-byte little-endian u64** (ADR-0025). One per drawn Juror.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 8   | `reveal(dispute, vote: u64, salt)`                                                                               | verifies `h`; records vote into `round.reveals[idx]`. **Vote gate by aggregation (ADR-0025):** `Plurality` requires `vote < num_options`; `Median` requires `vote != u64::MAX` (the no-reveal sentinel) — any other u64 fixed-point scalar passes. Vote-recording only — no fee, no ATA, no SPL transfer (ADR-0020). Participation fee credited at `finalize_round`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 9   | `appeal(dispute, new_evidence_hash)`                                                                             | **permissionless**; pays `N_new · fee_per_juror` + bond (in `fee_token`); opens a new round at `2N+1`. Max 3 appeals. Draws from the same `frozen_root`. `new_evidence_hash: [u8;32]` stashes per-round evidence at `evidence_hashes[current_round+1]`; `[0u8;32]` sentinel = no new evidence (milestone accord-qp7c).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 10  | `finalize_round` / `finalize_dispute`                                                                            | permissionless crank. `finalize_round`: **reveal-quorum gate** (ADR-0021) — if `reveal_count ≥ ceil(panel × reveal_threshold_bps / 10_000)` tallies per `terms.aggregation` (Plurality: modal option index — **a top-count tie (≥2 options share the max) is a non-decisive round → `RedrawEligible`** (ADR-0026; odd panels only prevent ties for binary full-reveal — ≥3 options and non-reveal splits still deadlock); Median: median of revealed scalars, even reveal-count → **upper middle** (`sorted[n/2]`), ADR-0025), credits `fees_earned += fee_per_juror` per revealer, decrements `fee_paid`, → `RoundResolved`; shortfall or tie → `RedrawEligible` (no credits, no result). `finalize_dispute`: redistributes slash pool (`stake_delta`, stake_token) + fee pool (`fees_earned`, fee_token) to coherent jurors (coherence per aggregation — see below); decrements `active_draws`.                                                                                                                     |
| 11  | `withdraw_fees`                                                                                                  | per-juror: pulls aggregate `fees_earned` from `fee_vault` → juror `fee_token` ATA. No `active_draws` gate, no timelock (ADR-0020).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 12  | `redraw(dispute)`                                                                                                | **permissionless** (ADR-0021); only from `RedrawEligible`. Slashes no-shows into `stake_delta` (pending, not `staked`), releases the round's `active_draws` + `slash_reserve`, bumps `round.draw_attempt` (orthogonal to `round_idx` — same panel size, no appeal consumed), clears the round → `Created`. On `draw_attempt + 1 ≥ max_draw_attempts` → `Failed`: refunds filer `fee_paid`, releases prior rounds' `active_draws`; slashes stand, bonds stay claimable via `claim_appeal_refund`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 12a | `reclaim_slot(juror_stake, leaf_path)`                                                                           | **permissionless** (RECLAIM-LEAF); pushes a fully-drained JurorStake's `tree_index` onto the Subaccord's free-list, blanking the leaf identity to `(default, 0)`. Closes the permanent-DoS hole where `next_index` only grows. Preconditions: `staked == 0`, `active_draws == 0`, `stake_delta == 0`, `fees_earned == 0`. A new staker then pops from the free list in `stake`, closing the freed account (rent → caller).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 13  | `get_ruling(dispute)`                                                                                            | read-only — returns `Option<u64>`: `None` until `Final`, then the winning option index (`Plurality`) or the final median (`Median`, ADR-0025). The Arbitrable reads it lazily via `Dispute::ruling()`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 14  | `pause()` / `unpause()`                                                                                          | multisig circuit-breaker (ADR-0007).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 15  | `prune_juror(juror, leaf_path)`                                                                                  | **permissionless crank** (PROG-ATTESTTION/ADR-0024). Evicts an **expired-credential** juror from a gated Subaccord's accumulator: the caller (any signer) supplies the juror's Merkle path + the expired SAS attestation in `remaining_accounts[0]` (the juror does NOT sign). Body mirrors `request_withdraw` for the **full** `staked` — zeros the leaf's selection weight, recomputes the root, banks the tokens into `pending_withdrawal`, decrements `staker_count`; the juror then completes the normal two-phase `withdraw`. Precondition: gated pool only; the attestation must have a real expiry that has passed (`expiry != 0 && expiry ≤ now`); no outstanding `slash_reserve` (⇔ no in-flight draws). A never-expiring credential (`expiry == 0`) can never be pruned.                                                                                                                                                                                                                                   |

`post_snapshot`, `challenge_snapshot`, and `finalize_snapshot` are removed from
the instruction surface (ADR-0012).

## Aggregation & coherence (ADR-0019/0025)

The tally rule is a per-Subaccord `Aggregation`, frozen onto `CaseTerms` at
filing; `finalize_round` and `settle_round_accounts` both `match` it (no
wildcard arm — a new variant is a compile error until its tally + coherence
arms land).

|                                | `Plurality`                                 | `Median` (ADR-0025)                                                                                                                                             |
| ------------------------------ | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Dispute filing                 | `2..=8` option hashes                       | **zero options** — the question is scalar                                                                                                                       |
| Vote (`reveal`)                | u64 option index; gate `vote < num_options` | u64 fixed-point scalar in **settlement-mint base units** (e.g. 6 decimals USDC); gate `vote != u64::MAX`                                                        |
| Round tally (`finalize_round`) | modal option index                          | **median** of the revealed values; even reveal-count (non-revealers shrink the panel) → the **upper middle** element `sorted[n/2]` (deterministic, biased high) |
| Coherence (settlement)         | exact: `vote == final_ruling`               | tolerance band: `\|vote − ruling\| · 10_000 ≤ ruling · coherence_tol_bps`                                                                                       |

`coherence_tol_bps` (default 100 = ±1% of the final median; `0` = exact match;
validated `≤ 10_000`) is immutable on the Subaccord — it defines the pool's
coherence game, so governance cannot loosen it under live disputes (it is
absent from `UpdatePayload`; the frozen `CaseTerms` copy governs each dispute).
`u64::MAX` is the universal no-reveal / not-set sentinel (`reveals` entries,
`Round.result`, `Dispute.final_ruling`); `Dispute::ruling()` returns
`Option<u64>`, `None` outside `Final`.

The commit preimage is shared by both rules: `hash(vote_le8 ‖ salt ‖ juror)` —
the vote serialized as 8 little-endian bytes (72 preimage bytes total), so a
scalar vote and an index vote commit identically.

## Dispute state machine

```
Created →(VRF committed, root frozen)→ Drawn → Review
   → Commit → Reveal → finalize_round
        ├─(quorum met + decisive tally)─→ RoundResolved →(appeal window, per-Subaccord terms.appeal_window)→ [Appeal: new round…] → Final → Closed
        └─(shortfall ─or─ Plurality top-count tie, ADR-0026)──→ RedrawEligible → redraw
                             ├─(draw_attempt+1 < max_draw_attempts)─→ Created (fresh seats, draw_attempt++)
                             └─(exhausted)─────────────────────────→ Failed (filer fee refunded; slashes stand)
```

`draw_attempt` (ADR-0021) is orthogonal to `round_idx`: a shortfall redraw re-seeds
the sortition for fresh seats at the **same** panel size without consuming an
appeal. `(round_idx, draw_attempt)` salts the seed
`hash(committed_vrf ‖ dispute ‖ round_idx ‖ draw_attempt ‖ seat ‖ retry)`. A
permissionless crank advances states when their windows elapse. Commit→reveal
opens as soon as `now ≥ commit_end` **OR** every drawn juror has committed
(`commit_count == juror_count` — the panel-full commit flips state straight to
`Reveal`, so a fully-committed panel needn't idle out the commit window); the
reveal window's `reveal_end` upper bound is unchanged. `finalize_round`
(reveal→resolved) likewise resolves early once every juror has revealed
(`reveal_count == juror_count`) — no need to wait out the reveal window. Odd
`min_jury_size` (default 3) keeps every appeal round odd
(3 / 7 / 15 / 31 at default; accord-9q3e generalizes the base), preventing
two-bloc full-panel splits — but **not** literal ties: N>2 options tie on a
full panel (3-3-1 of 7), and partial reveals keep the effective parity even;
those route to `RedrawEligible` (see row 10).

## Economics (Kleros-inherited; ADR-0020 two-mint split)

- **Two mints:** `staking_token` (collateral — sortition weight + slash exposure) and `fee_token` (compensation — fees + appeal bonds, USDC by convention).
- **Fee:** filer pays `min_jury_size · fee_per_juror` (round 1, in `fee_token`; accord-9q3e); appellant pays `N_new · fee_per_juror` + bond (in `fee_token`). Fees flow into the per-Subaccord `fee_vault`. `dispute.fee_paid` tracks ONLY the round-0 filing fee; the appeal fee + bond live in `AppealBond.amount` (bean accord-xftx — non-overlapping ownership).
- **Fee credit:** `finalize_round` credits each revealer `fees_earned += fee_per_juror` (every round) but decrements `dispute.fee_paid` ONLY for round 0, and ONLY if the reveal-quorum threshold is met (`reveal_count ≥ ceil(panel × reveal_threshold_bps / 10_000)`, ADR-0021) AND the tally is decisive — a Plurality top-count tie credits nothing (ADR-0026). A shortfall or tie credits nothing → the filer's single deposit suffices across the whole redraw ladder.
- **Slash:** each Incoherent Juror loses **`α · min_stake`** (flat, in `staking_token`) → written to `stake_delta`. _Incoherent_ is judged per aggregation against the **final** ruling (ADR-0025): `Plurality` = exact option match; `Median` = outside the `coherence_tol_bps` band (see [Aggregation & coherence](#aggregation--coherence-adr-00190025)).
- **Redistribution (two pools):** slash pool (`stake_token`) → coherent `stake_delta`; fee pool = non-revealer fees + forfeited bonds (`fee_token`) → coherent `fees_earned`. When no juror is coherent but some revealed, pools fall back to revealers (those who at least participated); zero reveals → surplus trapped in vault as protocol revenue (bean accord-aqmw). Equal split (integer div; remainder is protocol surplus).
- **`withdraw_fees`:** per-juror, pulls aggregate `fees_earned` from `fee_vault`. No `active_draws` gate, no timelock.
- **Cross-round settlement:** every round is re-settled against the **final** Ruling (Kleros §4.6, ADR-0018).
- **Fund invariant (parallel-ledger, bean accord-fdad):** Each Subaccord tracks four running accumulators — `fee_vault_deposited`/`fee_vault_withdrawn` (every `fee_token` SPL transfer in/out) and `stake_vault_deposited`/`stake_vault_withdrawn` (every `staking_token` SPL transfer in/out). The vault balance is a **derivable consequence**, always an exact `==`:

  ```
  separate-mint:  fee_vault.amount   == fee_vault_deposited − fee_vault_withdrawn
                  stake_vault.amount == stake_vault_deposited − stake_vault_withdrawn
  same-mint:      vault.amount == (fee_vault_deposited − fee_vault_withdrawn)
                               + (stake_vault_deposited − stake_vault_withdrawn)
  ```

  No inequalities. `withdraw_fees` pulls the juror's full `fees_earned` with **no gross-balance cap** — the fee-side net is provably ≥ Σ `fees_earned` (every credit was preceded by its backing deposit; refunds return only unconsumed portions). Slashing and `request_withdraw` are ledger-only and never touch the accumulators.

## Authority model

- **Subaccord:** `authority: Pubkey` (`default` ⇒ immutable; on-chain 48h timelock via propose/execute). `evidence_operator: Pubkey`.
- **Program upgrade:** Squads multisig → (post-sufficient-audit) `None` = frozen (ADR-0007).

## Evidence flow (ADR-0006)

```
claimant ──encrypt(evidence, evidence_operator_pubkey)──► encrypted blob ──► off-chain store
on-chain Accord: evidence_hash only
dispute filed + Jurors drawn (on-chain record)
   ▼
evidence_operator's open-source service: decrypt → re-encrypt per drawn Juror
   (+ optional per-Juror watermark → leak attribution) → deliver
   ▼
Juror decrypts, verifies cleartext vs on-chain evidence_hash
```

## Edge cases & defaults

- **Insufficient Jurors:** `create_dispute` / `appeal` **reverts** if the Subaccord's active distinct stakers < `min_jury_size`. (Alt — under-fill — rejected: breaks the Schelling jury.)
- **Ties (Plurality literal tie):** quorum met but argmax not unique ⇒ no tally, → `RedrawEligible` (ADR-0021 seam); revealers unslashed + unbilled, `redraw` re-seats the panel via `draw_attempt`. Odd panels prevent only two-bloc full-panel splits.
- **No Coherent Jurors in a round:** the round's pool defaults to the winning option's favour (Kleros §4.6 fn.10). _Flag for revisit._
- **VRF availability:** `request_vrf` retries with backoff if the VRF result isn't yet available.
- **Stale/fabricated Merkle path** (stake/unstake/draw_seat): reverts — the path is verified against the stored/frozen root; the root cannot be corrupted by a bad path. Indexer liveness dependency only (recompute + retry), not a correctness risk.
- **Pool size:** bounded by the Subaccord's fixed `depth` (`2^depth`; default 20 ≈ 1M seats), plus rent economics (each `JurorStake` ≈ 0.0007 SOL, paid by the juror) and indexer capacity. Outgrowing a depth = new Subaccord or v2 migration.

## Out of scope (v2+)

Accord token · dynamic params · further aggregation rules beyond `Plurality`/`Median` (IRV — ADR-0019; `Median` itself shipped, ADR-0025) · stake-weighted Subaccord self-governance (ADR-0005 alt) · Arcium encrypted vote-tally (Juror vote privacy) · any on-chain evidence crypto beyond the trusted-operator hash model · validity proof (SNARK) that the accumulator root was built correctly (ADR-0012 future).

## References

- `apps/docs/docs/adr/0012` on-chain stake accumulator · `0004` party-agnostic · `0005` Subaccord authority · `0006` evidence · `0007` upgrade · `0020` two-mint/two-vault economics · `0021` reveal-quorum + shortfall redraw · `0025` scalar voting (u64 votes, `Median`, coherence band) · `0026` Plurality tie → non-decisive round
- `apps/docs/docs/adr/0001` Schelling-over-hired-judges · `0002` per-Subaccord staking token (partially superseded by 0020)
- `CONTEXT.md` (domain language) · `PROJECT.md` (rationale) · `context/kleros-whitepaper.md` (prior art)
