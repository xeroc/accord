{
  "name": "@accord/evidence-daemon",
  "version": "0.1.0",
  "private": true,
  "description": "Accord Evidence Operator Daemon — off-chain decrypt/re-encrypt service (ADR-0011)",
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "lint": "tsc -p tsconfig.json --noEmit",
    "lint:fix": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "start": "bun run src/main.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@solana/kit": "^7.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.5.0"
  }
}
