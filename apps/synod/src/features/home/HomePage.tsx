/**
 * HomePage — scaffold stub.
 *
 * Replaced by the real home (hero + "Cases awaiting you" inbox + case
 * browser) in accord-hvf9. Lockup per milestone accord-daq8:
 * `SYNOD / Convene the verdict.`
 */
export function HomePage() {
  return (
    <section className="flex min-h-[60vh] flex-col items-center justify-center gap-4 text-center">
      <h1 className="font-mono text-4xl font-semibold tracking-tight text-amber">
        SYNOD
      </h1>
      <p className="text-lg text-muted-foreground">Convene the verdict.</p>
    </section>
  );
}
