---
const links = [
  { href: "https://docs.useaccord.xyz", label: "Docs" },
  { href: "https://t.me/useaccord", label: "Telegram" },
  { href: "https://github.com/xeroc/accord", label: "GitHub" },
];
---

<header class="border-b border-border/60">
  <div class="mx-auto flex h-16 max-w-5xl items-center justify-between px-6">
    <a href="/#hero" class="flex items-center gap-2.5" aria-label="Accord — home">
      <svg width="20" height="20" viewBox="0 0 32 32" aria-hidden="true" class="text-amber">
        <g stroke="currentColor" stroke-width="2.4" stroke-linecap="square" fill="none">
          <line x1="7.5" y1="8.5" x2="16" y2="16"></line>
          <line x1="24.5" y1="8.5" x2="16" y2="16"></line>
          <line x1="16" y1="25" x2="16" y2="16"></line>
        </g>
        <circle cx="16" cy="16" r="2.6" fill="currentColor"></circle>
      </svg>
      <span class="font-mono text-[15px] font-medium tracking-tight text-nearwhite">Accord</span>
      <span class="ml-2 hidden items-center gap-1.5 font-mono text-xs text-muted sm:inline-flex">
        <span class="h-1.5 w-1.5 rounded-full bg-amber"></span>v1 · build target
      </span>
    </a>
    <nav class="flex items-center gap-5 font-mono text-sm text-muted">
      {
        links.map((l) => (
          <a
            href={l.href}
            class="transition-colors hover:text-nearwhite"
            rel={l.href.startsWith("http") ? "noopener" : undefined}
          >
            {l.label}
          </a>
        ))
      }
    </nav>
  </div>
</header>
