# n8n webhook URL the waitlist form POSTs { email, type, timestamp } to.
# Mirrors chainsquad.com's VITE_N8N_WEBHOOK_URL. Copy to .env and set.
# Exposed to the client (Astro PUBLIC_ prefix), so it is not a secret.
PUBLIC_N8N_WEBHOOK_URL=
