/**
 * Subaccord lifecycle: create_subaccord, propose/execute_subaccord_update,
 * pause quartet (initialize_pause, pause, propose_unpause, execute_unpause).
 *
 * Filled by facade-method task. Each function takes the Accord client + args
 * and returns a planable/sendable instruction or the tx signature.
 */
