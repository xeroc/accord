/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  type Codec,
  type Decoder,
  type Encoder,
} from "@solana/kit";
import {
  getLeafClaimDecoder,
  getLeafClaimEncoder,
  getMSTNodeDecoder,
  getMSTNodeEncoder,
  type LeafClaim,
  type LeafClaimArgs,
  type MSTNode,
  type MSTNodeArgs,
} from ".";

/**
 * A drawn Juror's MST membership proof: the leaf claim (with cumulative stake),
 * the sibling nodes rootward, and the leaf's position in the tree. The on-chain
 * `draw` verifies each proof against the finalized snapshot root hash + total
 * stake, checks the sortition criterion (`cum_before ≤ r_i < cum_after`),
 * enforces the inflation guard (`JurorStake.amount ≥ leaf.stake`), and
 * enforces distinctness.
 */
export type JurorMembership = {
  leaf: LeafClaim;
  proof: Array<MSTNode>;
  index: number;
};

export type JurorMembershipArgs = {
  leaf: LeafClaimArgs;
  proof: Array<MSTNodeArgs>;
  index: number;
};

export function getJurorMembershipEncoder(): Encoder<JurorMembershipArgs> {
  return getStructEncoder([
    ["leaf", getLeafClaimEncoder()],
    ["proof", getArrayEncoder(getMSTNodeEncoder())],
    ["index", getU32Encoder()],
  ]);
}

export function getJurorMembershipDecoder(): Decoder<JurorMembership> {
  return getStructDecoder([
    ["leaf", getLeafClaimDecoder()],
    ["proof", getArrayDecoder(getMSTNodeDecoder())],
    ["index", getU32Decoder()],
  ]);
}

export function getJurorMembershipCodec(): Codec<
  JurorMembershipArgs,
  JurorMembership
> {
  return combineCodec(getJurorMembershipEncoder(), getJurorMembershipDecoder());
}
