/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getSnapshotStatusDecoder,
  getSnapshotStatusEncoder,
  type SnapshotStatus,
  type SnapshotStatusArgs,
} from "../types";

export const SNAPSHOT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  137, 213, 28, 133, 224, 161, 48, 108,
]);

export function getSnapshotDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SNAPSHOT_DISCRIMINATOR);
}

export type Snapshot = {
  discriminator: ReadonlyUint8Array;
  dispute: Address;
  roundIdx: number;
  merkleRoot: ReadonlyUint8Array;
  poster: Address;
  /** Bond = 1x the dispute's max-appeal fee (forfeited if the root is fraud). */
  bond: bigint;
  /** Unix timestamp after which an unchallenged root is final. */
  challengeDeadline: bigint;
  status: SnapshotStatus;
  bump: number;
  /**
   * Slot at which the snapshot was posted (ADR-0008 anchor-slot pattern).
   * `JurorStake.last_change_slot < anchor_slot` certifies the live amount
   * equals the anchor-time amount — the witness for omission and wrong-stake
   * fraud predicates.
   */
  anchorSlot: bigint;
  /**
   * MST root sum: the total stake committed to by the snapshot tree
   * (ADR-0009). Used for sortition: `r_i % total_stake` selects a juror.
   */
  totalStake: bigint;
};

export type SnapshotArgs = {
  dispute: Address;
  roundIdx: number;
  merkleRoot: ReadonlyUint8Array;
  poster: Address;
  /** Bond = 1x the dispute's max-appeal fee (forfeited if the root is fraud). */
  bond: number | bigint;
  /** Unix timestamp after which an unchallenged root is final. */
  challengeDeadline: number | bigint;
  status: SnapshotStatusArgs;
  bump: number;
  /**
   * Slot at which the snapshot was posted (ADR-0008 anchor-slot pattern).
   * `JurorStake.last_change_slot < anchor_slot` certifies the live amount
   * equals the anchor-time amount — the witness for omission and wrong-stake
   * fraud predicates.
   */
  anchorSlot: number | bigint;
  /**
   * MST root sum: the total stake committed to by the snapshot tree
   * (ADR-0009). Used for sortition: `r_i % total_stake` selects a juror.
   */
  totalStake: number | bigint;
};

/** Gets the encoder for {@link SnapshotArgs} account data. */
export function getSnapshotEncoder(): FixedSizeEncoder<SnapshotArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["dispute", getAddressEncoder()],
      ["roundIdx", getU32Encoder()],
      ["merkleRoot", fixEncoderSize(getBytesEncoder(), 32)],
      ["poster", getAddressEncoder()],
      ["bond", getU64Encoder()],
      ["challengeDeadline", getI64Encoder()],
      ["status", getSnapshotStatusEncoder()],
      ["bump", getU8Encoder()],
      ["anchorSlot", getU64Encoder()],
      ["totalStake", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SNAPSHOT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Snapshot} account data. */
export function getSnapshotDecoder(): FixedSizeDecoder<Snapshot> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["dispute", getAddressDecoder()],
    ["roundIdx", getU32Decoder()],
    ["merkleRoot", fixDecoderSize(getBytesDecoder(), 32)],
    ["poster", getAddressDecoder()],
    ["bond", getU64Decoder()],
    ["challengeDeadline", getI64Decoder()],
    ["status", getSnapshotStatusDecoder()],
    ["bump", getU8Decoder()],
    ["anchorSlot", getU64Decoder()],
    ["totalStake", getU64Decoder()],
  ]);
}

/** Gets the codec for {@link Snapshot} account data. */
export function getSnapshotCodec(): FixedSizeCodec<SnapshotArgs, Snapshot> {
  return combineCodec(getSnapshotEncoder(), getSnapshotDecoder());
}

export function decodeSnapshot<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Snapshot, TAddress>;
export function decodeSnapshot<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Snapshot, TAddress>;
export function decodeSnapshot<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Snapshot, TAddress> | MaybeAccount<Snapshot, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getSnapshotDecoder(),
  );
}

export async function fetchSnapshot<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Snapshot, TAddress>> {
  const maybeAccount = await fetchMaybeSnapshot(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeSnapshot<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Snapshot, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeSnapshot(maybeAccount);
}

export async function fetchAllSnapshot(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Snapshot>[]> {
  const maybeAccounts = await fetchAllMaybeSnapshot(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeSnapshot(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Snapshot>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeSnapshot(maybeAccount));
}

export function getSnapshotSize(): number {
  return 142;
}
