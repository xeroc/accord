/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const JUROR_STAKE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  139, 114, 205, 73, 247, 175, 133, 122,
]);

export function getJurorStakeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(JUROR_STAKE_DISCRIMINATOR);
}

export type JurorStake = {
  discriminator: ReadonlyUint8Array;
  subaccord: Address;
  juror: Address;
  /** Collateral (stake_token). Sortition weight + slash exposure. */
  staked: bigint;
  activeDraws: number;
  bump: number;
  /** Leaf position in the Subaccord accumulator (assigned at first stake). */
  treeIndex: number;
  /**
   * Net slash(-)/reward(+) accumulated from settlements, in `stake_token`.
   * Written by settlement instead of mutating `staked` — keeps the
   * accumulator root canonical. Folded into `staked` by the permissionless
   * `reconcile_stake` crank (which updates the root via a Merkle proof).
   */
  stakeDelta: bigint;
  /**
   * Pending slash exposure from all active draws. Incremented by
   * `draw_seat` (by `α·min_stake` from the dispute's terms), decremented
   * by settlement or cancel. Ensures the juror can always cover every
   * concurrent slash. `staked - slash_reserve` is the truly free stake.
   */
  slashReserve: bigint;
  /**
   * Timestamp of `request_withdraw` (0 = no pending request). The
   * accumulator root is already updated at request time; `withdraw` just
   * transfers tokens after `WITHDRAWAL_DELAY` + `active_draws == 0`.
   */
  withdrawRequestedAt: bigint;
  /**
   * Tokens locked in the vault pending `withdraw`. Set at `request_withdraw`,
   * consumed at `withdraw`.
   */
  pendingWithdrawal: bigint;
  /**
   * Aggregate earned fees (`fee_token`, ADR-0020). Credited at
   * `finalize_round` + settlement; withdrawn via the ungated
   * `withdraw_fees` instruction. No `active_draws` gate (fees are earned,
   * not at-risk capital).
   */
  feesEarned: bigint;
  /**
   * Next free index in the free-slot linked list (RECLAIM-LEAF).
   * `u32::MAX` = this account is NOT a free-list node (active juror, or never
   * reclaimed). Any other value = this slot is reclaimed and `next_free` is
   * the next free index after this one. Set by `reclaim_slot`, consumed by
   * `stake`.
   */
  nextFree: number;
};

export type JurorStakeArgs = {
  subaccord: Address;
  juror: Address;
  /** Collateral (stake_token). Sortition weight + slash exposure. */
  staked: number | bigint;
  activeDraws: number;
  bump: number;
  /** Leaf position in the Subaccord accumulator (assigned at first stake). */
  treeIndex: number;
  /**
   * Net slash(-)/reward(+) accumulated from settlements, in `stake_token`.
   * Written by settlement instead of mutating `staked` — keeps the
   * accumulator root canonical. Folded into `staked` by the permissionless
   * `reconcile_stake` crank (which updates the root via a Merkle proof).
   */
  stakeDelta: number | bigint;
  /**
   * Pending slash exposure from all active draws. Incremented by
   * `draw_seat` (by `α·min_stake` from the dispute's terms), decremented
   * by settlement or cancel. Ensures the juror can always cover every
   * concurrent slash. `staked - slash_reserve` is the truly free stake.
   */
  slashReserve: number | bigint;
  /**
   * Timestamp of `request_withdraw` (0 = no pending request). The
   * accumulator root is already updated at request time; `withdraw` just
   * transfers tokens after `WITHDRAWAL_DELAY` + `active_draws == 0`.
   */
  withdrawRequestedAt: number | bigint;
  /**
   * Tokens locked in the vault pending `withdraw`. Set at `request_withdraw`,
   * consumed at `withdraw`.
   */
  pendingWithdrawal: number | bigint;
  /**
   * Aggregate earned fees (`fee_token`, ADR-0020). Credited at
   * `finalize_round` + settlement; withdrawn via the ungated
   * `withdraw_fees` instruction. No `active_draws` gate (fees are earned,
   * not at-risk capital).
   */
  feesEarned: number | bigint;
  /**
   * Next free index in the free-slot linked list (RECLAIM-LEAF).
   * `u32::MAX` = this account is NOT a free-list node (active juror, or never
   * reclaimed). Any other value = this slot is reclaimed and `next_free` is
   * the next free index after this one. Set by `reclaim_slot`, consumed by
   * `stake`.
   */
  nextFree: number;
};

/** Gets the encoder for {@link JurorStakeArgs} account data. */
export function getJurorStakeEncoder(): FixedSizeEncoder<JurorStakeArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["subaccord", getAddressEncoder()],
      ["juror", getAddressEncoder()],
      ["staked", getU64Encoder()],
      ["activeDraws", getU32Encoder()],
      ["bump", getU8Encoder()],
      ["treeIndex", getU32Encoder()],
      ["stakeDelta", getI64Encoder()],
      ["slashReserve", getU64Encoder()],
      ["withdrawRequestedAt", getI64Encoder()],
      ["pendingWithdrawal", getU64Encoder()],
      ["feesEarned", getU64Encoder()],
      ["nextFree", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: JUROR_STAKE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link JurorStake} account data. */
export function getJurorStakeDecoder(): FixedSizeDecoder<JurorStake> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["subaccord", getAddressDecoder()],
    ["juror", getAddressDecoder()],
    ["staked", getU64Decoder()],
    ["activeDraws", getU32Decoder()],
    ["bump", getU8Decoder()],
    ["treeIndex", getU32Decoder()],
    ["stakeDelta", getI64Decoder()],
    ["slashReserve", getU64Decoder()],
    ["withdrawRequestedAt", getI64Decoder()],
    ["pendingWithdrawal", getU64Decoder()],
    ["feesEarned", getU64Decoder()],
    ["nextFree", getU32Decoder()],
  ]);
}

/** Gets the codec for {@link JurorStake} account data. */
export function getJurorStakeCodec(): FixedSizeCodec<
  JurorStakeArgs,
  JurorStake
> {
  return combineCodec(getJurorStakeEncoder(), getJurorStakeDecoder());
}

export function decodeJurorStake<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<JurorStake, TAddress>;
export function decodeJurorStake<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<JurorStake, TAddress>;
export function decodeJurorStake<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<JurorStake, TAddress> | MaybeAccount<JurorStake, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getJurorStakeDecoder(),
  );
}

export async function fetchJurorStake<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<JurorStake, TAddress>> {
  const maybeAccount = await fetchMaybeJurorStake(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeJurorStake<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<JurorStake, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeJurorStake(maybeAccount);
}

export async function fetchAllJurorStake(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<JurorStake>[]> {
  const maybeAccounts = await fetchAllMaybeJurorStake(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeJurorStake(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<JurorStake>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeJurorStake(maybeAccount));
}

export function getJurorStakeSize(): number {
  return 133;
}
