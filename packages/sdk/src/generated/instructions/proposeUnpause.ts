/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findAccordStatePda } from "../pdas";
import { ACCORD_PROGRAM_ADDRESS } from "../programs";

export const PROPOSE_UNPAUSE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [45, 136, 163, 143, 156, 57, 113, 132],
);

export function getProposeUnpauseDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_UNPAUSE_DISCRIMINATOR,
  );
}

export type ProposeUnpauseInstruction<
  TProgram extends string = typeof ACCORD_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAccordState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAccordState extends string
        ? WritableAccount<TAccountAccordState>
        : TAccountAccordState,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeUnpauseInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ProposeUnpauseInstructionDataArgs = {};

export function getProposeUnpauseInstructionDataEncoder(): FixedSizeEncoder<ProposeUnpauseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PROPOSE_UNPAUSE_DISCRIMINATOR }),
  );
}

export function getProposeUnpauseInstructionDataDecoder(): FixedSizeDecoder<ProposeUnpauseInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getProposeUnpauseInstructionDataCodec(): FixedSizeCodec<
  ProposeUnpauseInstructionDataArgs,
  ProposeUnpauseInstructionData
> {
  return combineCodec(
    getProposeUnpauseInstructionDataEncoder(),
    getProposeUnpauseInstructionDataDecoder(),
  );
}

export type ProposeUnpauseAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAccordState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  accordState?: Address<TAccountAccordState>;
};

export async function getProposeUnpauseInstructionAsync<
  TAccountAuthority extends string,
  TAccountAccordState extends string,
  TProgramAddress extends Address = typeof ACCORD_PROGRAM_ADDRESS,
>(
  input: ProposeUnpauseAsyncInput<TAccountAuthority, TAccountAccordState>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProposeUnpauseInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAccordState
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ACCORD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    accordState: { value: input.accordState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.accordState.value) {
    accounts.accordState.value = await findAccordStatePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("accordState", accounts.accordState),
    ],
    data: getProposeUnpauseInstructionDataEncoder().encode({}),
    programAddress,
  } as ProposeUnpauseInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAccordState
  >);
}

export type ProposeUnpauseInput<
  TAccountAuthority extends string = string,
  TAccountAccordState extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  accordState: Address<TAccountAccordState>;
};

export function getProposeUnpauseInstruction<
  TAccountAuthority extends string,
  TAccountAccordState extends string,
  TProgramAddress extends Address = typeof ACCORD_PROGRAM_ADDRESS,
>(
  input: ProposeUnpauseInput<TAccountAuthority, TAccountAccordState>,
  config?: { programAddress?: TProgramAddress },
): ProposeUnpauseInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAccordState
> {
  // Program address.
  const programAddress = config?.programAddress ?? ACCORD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    accordState: { value: input.accordState ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("accordState", accounts.accordState),
    ],
    data: getProposeUnpauseInstructionDataEncoder().encode({}),
    programAddress,
  } as ProposeUnpauseInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAccordState
  >);
}

export type ParsedProposeUnpauseInstruction<
  TProgram extends string = typeof ACCORD_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    accordState: TAccountMetas[1];
  };
  data: ProposeUnpauseInstructionData;
};

export function parseProposeUnpauseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeUnpauseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), accordState: getNextAccount() },
    data: getProposeUnpauseInstructionDataDecoder().decode(instruction.data),
  };
}
