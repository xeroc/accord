/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { ACCORD_PROGRAM_ADDRESS } from "../programs";

export const GET_RULING_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  196, 52, 54, 1, 180, 75, 86, 16,
]);

export function getGetRulingDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GET_RULING_DISCRIMINATOR);
}

export type GetRulingInstruction<
  TProgram extends string = typeof ACCORD_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountDispute extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountDispute extends string
        ? ReadonlyAccount<TAccountDispute>
        : TAccountDispute,
      ...TRemainingAccounts,
    ]
  >;

export type GetRulingInstructionData = { discriminator: ReadonlyUint8Array };

export type GetRulingInstructionDataArgs = {};

export function getGetRulingInstructionDataEncoder(): FixedSizeEncoder<GetRulingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: GET_RULING_DISCRIMINATOR }),
  );
}

export function getGetRulingInstructionDataDecoder(): FixedSizeDecoder<GetRulingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGetRulingInstructionDataCodec(): FixedSizeCodec<
  GetRulingInstructionDataArgs,
  GetRulingInstructionData
> {
  return combineCodec(
    getGetRulingInstructionDataEncoder(),
    getGetRulingInstructionDataDecoder(),
  );
}

export type GetRulingInput<
  TAccountCaller extends string = string,
  TAccountDispute extends string = string,
> = {
  /** Fee payer for the transaction (CPI caller or cranker). */
  caller: TransactionSigner<TAccountCaller>;
  dispute: Address<TAccountDispute>;
};

export function getGetRulingInstruction<
  TAccountCaller extends string,
  TAccountDispute extends string,
  TProgramAddress extends Address = typeof ACCORD_PROGRAM_ADDRESS,
>(
  input: GetRulingInput<TAccountCaller, TAccountDispute>,
  config?: { programAddress?: TProgramAddress },
): GetRulingInstruction<TProgramAddress, TAccountCaller, TAccountDispute> {
  // Program address.
  const programAddress = config?.programAddress ?? ACCORD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    dispute: { value: input.dispute ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("dispute", accounts.dispute),
    ],
    data: getGetRulingInstructionDataEncoder().encode({}),
    programAddress,
  } as GetRulingInstruction<TProgramAddress, TAccountCaller, TAccountDispute>);
}

export type ParsedGetRulingInstruction<
  TProgram extends string = typeof ACCORD_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Fee payer for the transaction (CPI caller or cranker). */
    caller: TAccountMetas[0];
    dispute: TAccountMetas[1];
  };
  data: GetRulingInstructionData;
};

export function parseGetRulingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGetRulingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { caller: getNextAccount(), dispute: getNextAccount() },
    data: getGetRulingInstructionDataDecoder().decode(instruction.data),
  };
}
