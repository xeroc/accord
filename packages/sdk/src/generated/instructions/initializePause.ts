/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findPauseStatePda } from "../pdas";
import { ACCORD_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_PAUSE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([70, 32, 55, 248, 231, 46, 123, 153]);

export function getInitializePauseDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_PAUSE_DISCRIMINATOR,
  );
}

export type InitializePauseInstruction<
  TProgram extends string = typeof ACCORD_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPauseState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPauseState extends string
        ? WritableAccount<TAccountPauseState>
        : TAccountPauseState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializePauseInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializePauseInstructionDataArgs = {};

export function getInitializePauseInstructionDataEncoder(): FixedSizeEncoder<InitializePauseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: INITIALIZE_PAUSE_DISCRIMINATOR }),
  );
}

export function getInitializePauseInstructionDataDecoder(): FixedSizeDecoder<InitializePauseInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializePauseInstructionDataCodec(): FixedSizeCodec<
  InitializePauseInstructionDataArgs,
  InitializePauseInstructionData
> {
  return combineCodec(
    getInitializePauseInstructionDataEncoder(),
    getInitializePauseInstructionDataDecoder(),
  );
}

export type InitializePauseAsyncInput<
  TAccountAuthority extends string = string,
  TAccountPauseState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  pauseState?: Address<TAccountPauseState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializePauseInstructionAsync<
  TAccountAuthority extends string,
  TAccountPauseState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ACCORD_PROGRAM_ADDRESS,
>(
  input: InitializePauseAsyncInput<
    TAccountAuthority,
    TAccountPauseState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializePauseInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPauseState,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ACCORD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    pauseState: { value: input.pauseState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.pauseState.value) {
    accounts.pauseState.value = await findPauseStatePda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("pauseState", accounts.pauseState),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializePauseInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializePauseInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPauseState,
    TAccountSystemProgram
  >);
}

export type InitializePauseInput<
  TAccountAuthority extends string = string,
  TAccountPauseState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  pauseState: Address<TAccountPauseState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializePauseInstruction<
  TAccountAuthority extends string,
  TAccountPauseState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ACCORD_PROGRAM_ADDRESS,
>(
  input: InitializePauseInput<
    TAccountAuthority,
    TAccountPauseState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializePauseInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountPauseState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ACCORD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    pauseState: { value: input.pauseState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("pauseState", accounts.pauseState),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializePauseInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializePauseInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountPauseState,
    TAccountSystemProgram
  >);
}

export type ParsedInitializePauseInstruction<
  TProgram extends string = typeof ACCORD_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    pauseState: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializePauseInstructionData;
};

export function parseInitializePauseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializePauseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      pauseState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializePauseInstructionDataDecoder().decode(instruction.data),
  };
}
