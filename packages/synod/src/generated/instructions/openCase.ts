/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findCasePda } from "../pdas";
import { SYNOD_PROGRAM_ADDRESS } from "../programs";

export const OPEN_CASE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  249, 236, 95, 19, 245, 178, 92, 13,
]);

export function getOpenCaseDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(OPEN_CASE_DISCRIMINATOR);
}

export type OpenCaseInstruction<
  TProgram extends string = typeof SYNOD_PROGRAM_ADDRESS,
  TAccountOpener extends string | AccountMeta<string> = string,
  TAccountSubaccord extends string | AccountMeta<string> = string,
  TAccountCase extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOpener extends string
        ? WritableSignerAccount<TAccountOpener> &
            AccountSignerMeta<TAccountOpener>
        : TAccountOpener,
      TAccountSubaccord extends string
        ? ReadonlyAccount<TAccountSubaccord>
        : TAccountSubaccord,
      TAccountCase extends string
        ? WritableAccount<TAccountCase>
        : TAccountCase,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type OpenCaseInstructionData = {
  discriminator: ReadonlyUint8Array;
  parties: Array<Address>;
  stake: bigint;
  joinDeadline: bigint;
  nonce: bigint;
};

export type OpenCaseInstructionDataArgs = {
  parties: Array<Address>;
  stake: number | bigint;
  joinDeadline: number | bigint;
  nonce: number | bigint;
};

export function getOpenCaseInstructionDataEncoder(): Encoder<OpenCaseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["parties", getArrayEncoder(getAddressEncoder())],
      ["stake", getU64Encoder()],
      ["joinDeadline", getI64Encoder()],
      ["nonce", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: OPEN_CASE_DISCRIMINATOR }),
  );
}

export function getOpenCaseInstructionDataDecoder(): Decoder<OpenCaseInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["parties", getArrayDecoder(getAddressDecoder())],
    ["stake", getU64Decoder()],
    ["joinDeadline", getI64Decoder()],
    ["nonce", getU64Decoder()],
  ]);
}

export function getOpenCaseInstructionDataCodec(): Codec<
  OpenCaseInstructionDataArgs,
  OpenCaseInstructionData
> {
  return combineCodec(
    getOpenCaseInstructionDataEncoder(),
    getOpenCaseInstructionDataDecoder(),
  );
}

export type OpenCaseAsyncInput<
  TAccountOpener extends string = string,
  TAccountSubaccord extends string = string,
  TAccountCase extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  opener: TransactionSigner<TAccountOpener>;
  /**
   * The hosting Accord court. Read ONCE here for `aggregation`,
   * `min_jury_size`, `fee_per_juror`; the derived fee is frozen onto the
   * case. Chosen freely by the opener (no seed link).
   */
  subaccord: Address<TAccountSubaccord>;
  case?: Address<TAccountCase>;
  systemProgram?: Address<TAccountSystemProgram>;
  parties: OpenCaseInstructionDataArgs["parties"];
  stake: OpenCaseInstructionDataArgs["stake"];
  joinDeadline: OpenCaseInstructionDataArgs["joinDeadline"];
  nonce: OpenCaseInstructionDataArgs["nonce"];
};

export async function getOpenCaseInstructionAsync<
  TAccountOpener extends string,
  TAccountSubaccord extends string,
  TAccountCase extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SYNOD_PROGRAM_ADDRESS,
>(
  input: OpenCaseAsyncInput<
    TAccountOpener,
    TAccountSubaccord,
    TAccountCase,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  OpenCaseInstruction<
    TProgramAddress,
    TAccountOpener,
    TAccountSubaccord,
    TAccountCase,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SYNOD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    opener: { value: input.opener ?? null, isWritable: true },
    subaccord: { value: input.subaccord ?? null, isWritable: false },
    case: { value: input.case ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.case.value) {
    accounts.case.value = await findCasePda({
      opener: getAddressFromResolvedInstructionAccount(
        "opener",
        accounts.opener.value,
      ),
      nonce: getNonNullResolvedInstructionInput("nonce", args.nonce),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("opener", accounts.opener),
      getAccountMeta("subaccord", accounts.subaccord),
      getAccountMeta("case", accounts.case),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getOpenCaseInstructionDataEncoder().encode(
      args as OpenCaseInstructionDataArgs,
    ),
    programAddress,
  } as OpenCaseInstruction<
    TProgramAddress,
    TAccountOpener,
    TAccountSubaccord,
    TAccountCase,
    TAccountSystemProgram
  >);
}

export type OpenCaseInput<
  TAccountOpener extends string = string,
  TAccountSubaccord extends string = string,
  TAccountCase extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  opener: TransactionSigner<TAccountOpener>;
  /**
   * The hosting Accord court. Read ONCE here for `aggregation`,
   * `min_jury_size`, `fee_per_juror`; the derived fee is frozen onto the
   * case. Chosen freely by the opener (no seed link).
   */
  subaccord: Address<TAccountSubaccord>;
  case: Address<TAccountCase>;
  systemProgram?: Address<TAccountSystemProgram>;
  parties: OpenCaseInstructionDataArgs["parties"];
  stake: OpenCaseInstructionDataArgs["stake"];
  joinDeadline: OpenCaseInstructionDataArgs["joinDeadline"];
  nonce: OpenCaseInstructionDataArgs["nonce"];
};

export function getOpenCaseInstruction<
  TAccountOpener extends string,
  TAccountSubaccord extends string,
  TAccountCase extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SYNOD_PROGRAM_ADDRESS,
>(
  input: OpenCaseInput<
    TAccountOpener,
    TAccountSubaccord,
    TAccountCase,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): OpenCaseInstruction<
  TProgramAddress,
  TAccountOpener,
  TAccountSubaccord,
  TAccountCase,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SYNOD_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    opener: { value: input.opener ?? null, isWritable: true },
    subaccord: { value: input.subaccord ?? null, isWritable: false },
    case: { value: input.case ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("opener", accounts.opener),
      getAccountMeta("subaccord", accounts.subaccord),
      getAccountMeta("case", accounts.case),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getOpenCaseInstructionDataEncoder().encode(
      args as OpenCaseInstructionDataArgs,
    ),
    programAddress,
  } as OpenCaseInstruction<
    TProgramAddress,
    TAccountOpener,
    TAccountSubaccord,
    TAccountCase,
    TAccountSystemProgram
  >);
}

export type ParsedOpenCaseInstruction<
  TProgram extends string = typeof SYNOD_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    opener: TAccountMetas[0];
    /**
     * The hosting Accord court. Read ONCE here for `aggregation`,
     * `min_jury_size`, `fee_per_juror`; the derived fee is frozen onto the
     * case. Chosen freely by the opener (no seed link).
     */
    subaccord: TAccountMetas[1];
    case: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: OpenCaseInstructionData;
};

export function parseOpenCaseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedOpenCaseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      opener: getNextAccount(),
      subaccord: getNextAccount(),
      case: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getOpenCaseInstructionDataDecoder().decode(instruction.data),
  };
}
