/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getEnumDecoder,
  getEnumEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Lifecycle of a case (SPEC §Case state machine).
 *
 * v1 ships three variants. `Refunding` is deliberately NOT a variant: a roster
 * miss refunds every joined party inside `refund_roster_miss` and lands
 * directly in `Closed` (per-party `paid_out` bits carry the idempotency).
 */
export enum CaseState {
  Opening,
  Live,
  Closed,
}

export type CaseStateArgs = CaseState;

export function getCaseStateEncoder(): FixedSizeEncoder<CaseStateArgs> {
  return getEnumEncoder(CaseState);
}

export function getCaseStateDecoder(): FixedSizeDecoder<CaseState> {
  return getEnumDecoder(CaseState);
}

export function getCaseStateCodec(): FixedSizeCodec<CaseStateArgs, CaseState> {
  return combineCodec(getCaseStateEncoder(), getCaseStateDecoder());
}
