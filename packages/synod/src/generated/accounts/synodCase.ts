/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getCaseStateDecoder,
  getCaseStateEncoder,
  type CaseState,
  type CaseStateArgs,
} from "../types";

export const SYNOD_CASE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  70, 137, 118, 159, 197, 200, 16, 211,
]);

export function getSynodCaseDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SYNOD_CASE_DISCRIMINATOR);
}

export type SynodCase = {
  discriminator: ReadonlyUint8Array;
  /** The hosting Accord Subaccord (fee_token + frozen fee source). */
  subaccord: Address;
  /**
   * Party roster in naming order; opener = index 0. Slots ≥ `party_count`
   * are `Pubkey::default()` padding.
   */
  parties: Array<Address>;
  /** Number of live parties, `MIN_PARTIES..=MAX_PARTIES` (2..=7). */
  partyCount: number;
  /** Bitmask of joined parties: bit `i` set ⇒ `parties[i]` staked `S`. */
  joined: number;
  /** Per-party stake `S` (`subaccord.fee_token`). The only economic dial. */
  stake: bigint;
  /**
   * **Frozen at open**: `initial_num_jurors · fee_per_juror` read once from
   * the Subaccord. Never re-read at file — governance can't shift the deal
   * mid-window (SPEC §Account model).
   */
  fee: bigint;
  /**
   * Unix timestamp after which an incomplete roster refunds (Clock
   * unix_time).
   */
  joinDeadline: bigint;
  /**
   * Per-party evidence hash, written at `join` (slot frozen at join; late
   * evidence rides appeal rounds, ADR-0023, via independent appeal).
   * Slots ≥ `party_count` are `[0u8; 32]` padding.
   */
  evidence: Array<ReadonlyUint8Array>;
  /**
   * The Accord `Dispute` PDA (`["dispute", case, 0]`); `Pubkey::default()`
   * sentinel until `file_dispute` binds it (immutable after — SPEC
   * §Invariants 2).
   */
  dispute: Address;
  /**
   * Bitmask of settled payouts: bit `i` set ⇒ `parties[i]` received its
   * claim. Idempotency for `refund_roster_miss` + `claim` (pull-only, SPEC
   * §Invariants 3).
   */
  paidOut: number;
  /** Current lifecycle stage. */
  state: CaseState;
  bump: number;
};

export type SynodCaseArgs = {
  /** The hosting Accord Subaccord (fee_token + frozen fee source). */
  subaccord: Address;
  /**
   * Party roster in naming order; opener = index 0. Slots ≥ `party_count`
   * are `Pubkey::default()` padding.
   */
  parties: Array<Address>;
  /** Number of live parties, `MIN_PARTIES..=MAX_PARTIES` (2..=7). */
  partyCount: number;
  /** Bitmask of joined parties: bit `i` set ⇒ `parties[i]` staked `S`. */
  joined: number;
  /** Per-party stake `S` (`subaccord.fee_token`). The only economic dial. */
  stake: number | bigint;
  /**
   * **Frozen at open**: `initial_num_jurors · fee_per_juror` read once from
   * the Subaccord. Never re-read at file — governance can't shift the deal
   * mid-window (SPEC §Account model).
   */
  fee: number | bigint;
  /**
   * Unix timestamp after which an incomplete roster refunds (Clock
   * unix_time).
   */
  joinDeadline: number | bigint;
  /**
   * Per-party evidence hash, written at `join` (slot frozen at join; late
   * evidence rides appeal rounds, ADR-0023, via independent appeal).
   * Slots ≥ `party_count` are `[0u8; 32]` padding.
   */
  evidence: Array<ReadonlyUint8Array>;
  /**
   * The Accord `Dispute` PDA (`["dispute", case, 0]`); `Pubkey::default()`
   * sentinel until `file_dispute` binds it (immutable after — SPEC
   * §Invariants 2).
   */
  dispute: Address;
  /**
   * Bitmask of settled payouts: bit `i` set ⇒ `parties[i]` received its
   * claim. Idempotency for `refund_roster_miss` + `claim` (pull-only, SPEC
   * §Invariants 3).
   */
  paidOut: number;
  /** Current lifecycle stage. */
  state: CaseStateArgs;
  bump: number;
};

/** Gets the encoder for {@link SynodCaseArgs} account data. */
export function getSynodCaseEncoder(): FixedSizeEncoder<SynodCaseArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["subaccord", getAddressEncoder()],
      ["parties", getArrayEncoder(getAddressEncoder(), { size: 7 })],
      ["partyCount", getU8Encoder()],
      ["joined", getU8Encoder()],
      ["stake", getU64Encoder()],
      ["fee", getU64Encoder()],
      ["joinDeadline", getI64Encoder()],
      [
        "evidence",
        getArrayEncoder(fixEncoderSize(getBytesEncoder(), 32), { size: 7 }),
      ],
      ["dispute", getAddressEncoder()],
      ["paidOut", getU8Encoder()],
      ["state", getCaseStateEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SYNOD_CASE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link SynodCase} account data. */
export function getSynodCaseDecoder(): FixedSizeDecoder<SynodCase> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["subaccord", getAddressDecoder()],
    ["parties", getArrayDecoder(getAddressDecoder(), { size: 7 })],
    ["partyCount", getU8Decoder()],
    ["joined", getU8Decoder()],
    ["stake", getU64Decoder()],
    ["fee", getU64Decoder()],
    ["joinDeadline", getI64Decoder()],
    [
      "evidence",
      getArrayDecoder(fixDecoderSize(getBytesDecoder(), 32), { size: 7 }),
    ],
    ["dispute", getAddressDecoder()],
    ["paidOut", getU8Decoder()],
    ["state", getCaseStateDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link SynodCase} account data. */
export function getSynodCaseCodec(): FixedSizeCodec<SynodCaseArgs, SynodCase> {
  return combineCodec(getSynodCaseEncoder(), getSynodCaseDecoder());
}

export function decodeSynodCase<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<SynodCase, TAddress>;
export function decodeSynodCase<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<SynodCase, TAddress>;
export function decodeSynodCase<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<SynodCase, TAddress> | MaybeAccount<SynodCase, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getSynodCaseDecoder(),
  );
}

export async function fetchSynodCase<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<SynodCase, TAddress>> {
  const maybeAccount = await fetchMaybeSynodCase(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeSynodCase<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<SynodCase, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeSynodCase(maybeAccount);
}

export async function fetchAllSynodCase(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<SynodCase>[]> {
  const maybeAccounts = await fetchAllMaybeSynodCase(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeSynodCase(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<SynodCase>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeSynodCase(maybeAccount));
}

export function getSynodCaseSize(): number {
  return 549;
}
