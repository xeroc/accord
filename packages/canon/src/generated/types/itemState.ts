/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getEnumDecoder,
  getEnumEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * Lifecycle of a curated item (SPEC §Item state machine).
 *
 * v1 ships five variants. `WithdrawPending` (a submitter-initiated delist) is
 * challengeable: a challenge re-enters the dispute path and the item is
 * `Removed` either way — the dispute only decides whether the submitter keeps
 * the deposit or forfeits it to the challenger. There is deliberately no
 * `Withdrawn` variant: a completed withdrawal lands the item in `Removed`.
 */
export enum ItemState {
  Pending,
  Listed,
  Removed,
  WithdrawPending,
  Disputed,
}

export type ItemStateArgs = ItemState;

export function getItemStateEncoder(): FixedSizeEncoder<ItemStateArgs> {
  return getEnumEncoder(ItemState);
}

export function getItemStateDecoder(): FixedSizeDecoder<ItemState> {
  return getEnumDecoder(ItemState);
}

export function getItemStateCodec(): FixedSizeCodec<ItemStateArgs, ItemState> {
  return combineCodec(getItemStateEncoder(), getItemStateDecoder());
}
