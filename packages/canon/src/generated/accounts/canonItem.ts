/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getItemStateDecoder,
  getItemStateEncoder,
  type ItemState,
  type ItemStateArgs,
} from "../types";

export const CANON_ITEM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  101, 145, 226, 218, 86, 208, 23, 106,
]);

export function getCanonItemDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CANON_ITEM_DISCRIMINATOR);
}

export type CanonItem = {
  discriminator: ReadonlyUint8Array;
  /** The curated address — a PDA owned by `CanonList.list_program`. */
  account: Address;
  /** Back-ref to the owning `CanonList` (seed component). */
  list: Address;
  /** Who filed the item (locks the deposit; sole withdrawer). */
  submitter: Address;
  /** Current lifecycle stage. */
  state: ItemState;
  /**
   * Skin accumulated on this item, in `fee_mint`. Starts at `submit_deposit`;
   * grows by each forfeited `challenge_stake` on a `keep` ruling (progressive
   * protection); paid out as the bounty on `remove`.
   */
  accumulatedStake: bigint;
  /** `submit_item` timestamp (Clock unix_time). Gates `advance_pending`. */
  submittedAt: bigint;
  /**
   * Total challenges ever filed against this item (progressive-protection
   * history depth).
   */
  challengeCount: number;
  /**
   * The Accord `Dispute` PDA resolving the current challenge
   * (`Pubkey::default()` when not disputed).
   */
  activeDispute: Address;
  /**
   * The challenger who filed the current dispute (`Pubkey::default()` when
   * not disputed; the bounty payee on `remove`).
   */
  challenger: Address;
  /**
   * Stake the challenger locked for the current dispute, in `fee_mint`
   * (`challenge_pct × accumulated_stake`); 0 when not disputed.
   */
  challengeStake: bigint;
  /**
   * Timestamp the current challenge was filed (Clock unix_time); 0 when not
   * disputed.
   */
  challengedAt: bigint;
  /**
   * When the submitter opened a withdrawal (`request_withdrawal`); gates
   * `advance_withdrawal`. `None` unless `state == WithdrawPending`.
   */
  withdrawalRequestedAt: Option<bigint>;
  bump: number;
};

export type CanonItemArgs = {
  /** The curated address — a PDA owned by `CanonList.list_program`. */
  account: Address;
  /** Back-ref to the owning `CanonList` (seed component). */
  list: Address;
  /** Who filed the item (locks the deposit; sole withdrawer). */
  submitter: Address;
  /** Current lifecycle stage. */
  state: ItemStateArgs;
  /**
   * Skin accumulated on this item, in `fee_mint`. Starts at `submit_deposit`;
   * grows by each forfeited `challenge_stake` on a `keep` ruling (progressive
   * protection); paid out as the bounty on `remove`.
   */
  accumulatedStake: number | bigint;
  /** `submit_item` timestamp (Clock unix_time). Gates `advance_pending`. */
  submittedAt: number | bigint;
  /**
   * Total challenges ever filed against this item (progressive-protection
   * history depth).
   */
  challengeCount: number;
  /**
   * The Accord `Dispute` PDA resolving the current challenge
   * (`Pubkey::default()` when not disputed).
   */
  activeDispute: Address;
  /**
   * The challenger who filed the current dispute (`Pubkey::default()` when
   * not disputed; the bounty payee on `remove`).
   */
  challenger: Address;
  /**
   * Stake the challenger locked for the current dispute, in `fee_mint`
   * (`challenge_pct × accumulated_stake`); 0 when not disputed.
   */
  challengeStake: number | bigint;
  /**
   * Timestamp the current challenge was filed (Clock unix_time); 0 when not
   * disputed.
   */
  challengedAt: number | bigint;
  /**
   * When the submitter opened a withdrawal (`request_withdrawal`); gates
   * `advance_withdrawal`. `None` unless `state == WithdrawPending`.
   */
  withdrawalRequestedAt: OptionOrNullable<number | bigint>;
  bump: number;
};

/** Gets the encoder for {@link CanonItemArgs} account data. */
export function getCanonItemEncoder(): Encoder<CanonItemArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["account", getAddressEncoder()],
      ["list", getAddressEncoder()],
      ["submitter", getAddressEncoder()],
      ["state", getItemStateEncoder()],
      ["accumulatedStake", getU64Encoder()],
      ["submittedAt", getI64Encoder()],
      ["challengeCount", getU32Encoder()],
      ["activeDispute", getAddressEncoder()],
      ["challenger", getAddressEncoder()],
      ["challengeStake", getU64Encoder()],
      ["challengedAt", getI64Encoder()],
      ["withdrawalRequestedAt", getOptionEncoder(getI64Encoder())],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CANON_ITEM_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link CanonItem} account data. */
export function getCanonItemDecoder(): Decoder<CanonItem> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["account", getAddressDecoder()],
    ["list", getAddressDecoder()],
    ["submitter", getAddressDecoder()],
    ["state", getItemStateDecoder()],
    ["accumulatedStake", getU64Decoder()],
    ["submittedAt", getI64Decoder()],
    ["challengeCount", getU32Decoder()],
    ["activeDispute", getAddressDecoder()],
    ["challenger", getAddressDecoder()],
    ["challengeStake", getU64Decoder()],
    ["challengedAt", getI64Decoder()],
    ["withdrawalRequestedAt", getOptionDecoder(getI64Decoder())],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link CanonItem} account data. */
export function getCanonItemCodec(): Codec<CanonItemArgs, CanonItem> {
  return combineCodec(getCanonItemEncoder(), getCanonItemDecoder());
}

export function decodeCanonItem<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<CanonItem, TAddress>;
export function decodeCanonItem<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<CanonItem, TAddress>;
export function decodeCanonItem<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<CanonItem, TAddress> | MaybeAccount<CanonItem, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getCanonItemDecoder(),
  );
}

export async function fetchCanonItem<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<CanonItem, TAddress>> {
  const maybeAccount = await fetchMaybeCanonItem(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCanonItem<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<CanonItem, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeCanonItem(maybeAccount);
}

export async function fetchAllCanonItem(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<CanonItem>[]> {
  const maybeAccounts = await fetchAllMaybeCanonItem(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeCanonItem(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<CanonItem>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeCanonItem(maybeAccount));
}
