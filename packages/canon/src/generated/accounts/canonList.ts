/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CANON_LIST_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  141, 245, 12, 195, 239, 156, 113, 88,
]);

export function getCanonListDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CANON_LIST_DISCRIMINATOR);
}

export type CanonList = {
  discriminator: ReadonlyUint8Array;
  /** Seed component + list creator. */
  creator: Address;
  /**
   * Juror collateral mint — staked/sashed in the backing Subaccord
   * (ADR-0002/0020). May equal `fee_mint`.
   */
  stakeMint: Address;
  /**
   * Compensation mint — Canon registry economics (deposits, bounties) + the
   * Accord fee (ADR-0020). Default USDC.
   */
  feeMint: Address;
  /**
   * **Immutable.** The program whose accounts this list curates. The item's
   * `account.owner` must equal this at `submit_item`.
   * `Pubkey::default()` => ownership check disabled (curate arbitrary base58
   * data).
   */
  listProgram: Address;
  /**
   * **Immutable.** Public listing-criteria doc hash jurors apply. Seed
   * component. Passed to Accord as the backing Subaccord's `risk_type`.
   */
  rulesHash: ReadonlyUint8Array;
  /** The 1:1 backing Accord court that adjudicates this list's item disputes. */
  subaccord: Address;
  /** Permanent skin-in-the-game locked at `submit_item`, in `fee_mint`. */
  submitDeposit: bigint;
  /**
   * Challenger stake as a fraction (bps) of the item's `accumulated_stake`.
   * Bounded by `MAX_CHALLENGE_PCT_BPS`.
   */
  challengePct: number;
  /** Seconds an item sits `Pending` before auto-listing if unchallenged. */
  listingWindow: bigint;
  /** Seconds the `WithdrawPending` fraud-challenge window stays open. */
  withdrawalTimelock: bigint;
  /**
   * Canon governance multisig (set at `create_list`); passed as the backing
   * Subaccord's authority so it controls dispute-param retuning.
   */
  authority: Address;
  /**
   * Count of `CanonItem`s ever filed under this list (PDA-distinctness
   * guarantee; monotonic, never decremented).
   */
  itemCount: number;
  bump: number;
};

export type CanonListArgs = {
  /** Seed component + list creator. */
  creator: Address;
  /**
   * Juror collateral mint — staked/sashed in the backing Subaccord
   * (ADR-0002/0020). May equal `fee_mint`.
   */
  stakeMint: Address;
  /**
   * Compensation mint — Canon registry economics (deposits, bounties) + the
   * Accord fee (ADR-0020). Default USDC.
   */
  feeMint: Address;
  /**
   * **Immutable.** The program whose accounts this list curates. The item's
   * `account.owner` must equal this at `submit_item`.
   * `Pubkey::default()` => ownership check disabled (curate arbitrary base58
   * data).
   */
  listProgram: Address;
  /**
   * **Immutable.** Public listing-criteria doc hash jurors apply. Seed
   * component. Passed to Accord as the backing Subaccord's `risk_type`.
   */
  rulesHash: ReadonlyUint8Array;
  /** The 1:1 backing Accord court that adjudicates this list's item disputes. */
  subaccord: Address;
  /** Permanent skin-in-the-game locked at `submit_item`, in `fee_mint`. */
  submitDeposit: number | bigint;
  /**
   * Challenger stake as a fraction (bps) of the item's `accumulated_stake`.
   * Bounded by `MAX_CHALLENGE_PCT_BPS`.
   */
  challengePct: number;
  /** Seconds an item sits `Pending` before auto-listing if unchallenged. */
  listingWindow: number | bigint;
  /** Seconds the `WithdrawPending` fraud-challenge window stays open. */
  withdrawalTimelock: number | bigint;
  /**
   * Canon governance multisig (set at `create_list`); passed as the backing
   * Subaccord's authority so it controls dispute-param retuning.
   */
  authority: Address;
  /**
   * Count of `CanonItem`s ever filed under this list (PDA-distinctness
   * guarantee; monotonic, never decremented).
   */
  itemCount: number;
  bump: number;
};

/** Gets the encoder for {@link CanonListArgs} account data. */
export function getCanonListEncoder(): FixedSizeEncoder<CanonListArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["creator", getAddressEncoder()],
      ["stakeMint", getAddressEncoder()],
      ["feeMint", getAddressEncoder()],
      ["listProgram", getAddressEncoder()],
      ["rulesHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["subaccord", getAddressEncoder()],
      ["submitDeposit", getU64Encoder()],
      ["challengePct", getU16Encoder()],
      ["listingWindow", getU64Encoder()],
      ["withdrawalTimelock", getU64Encoder()],
      ["authority", getAddressEncoder()],
      ["itemCount", getU32Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CANON_LIST_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link CanonList} account data. */
export function getCanonListDecoder(): FixedSizeDecoder<CanonList> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["creator", getAddressDecoder()],
    ["stakeMint", getAddressDecoder()],
    ["feeMint", getAddressDecoder()],
    ["listProgram", getAddressDecoder()],
    ["rulesHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["subaccord", getAddressDecoder()],
    ["submitDeposit", getU64Decoder()],
    ["challengePct", getU16Decoder()],
    ["listingWindow", getU64Decoder()],
    ["withdrawalTimelock", getU64Decoder()],
    ["authority", getAddressDecoder()],
    ["itemCount", getU32Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link CanonList} account data. */
export function getCanonListCodec(): FixedSizeCodec<CanonListArgs, CanonList> {
  return combineCodec(getCanonListEncoder(), getCanonListDecoder());
}

export function decodeCanonList<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<CanonList, TAddress>;
export function decodeCanonList<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<CanonList, TAddress>;
export function decodeCanonList<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<CanonList, TAddress> | MaybeAccount<CanonList, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getCanonListDecoder(),
  );
}

export async function fetchCanonList<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<CanonList, TAddress>> {
  const maybeAccount = await fetchMaybeCanonList(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeCanonList<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<CanonList, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeCanonList(maybeAccount);
}

export async function fetchAllCanonList(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<CanonList>[]> {
  const maybeAccounts = await fetchAllMaybeCanonList(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeCanonList(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<CanonList>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeCanonList(maybeAccount));
}

export function getCanonListSize(): number {
  return 263;
}
