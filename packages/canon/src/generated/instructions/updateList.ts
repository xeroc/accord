/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { CANON_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_LIST_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  121, 92, 164, 155, 59, 155, 100, 38,
]);

export function getUpdateListDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_LIST_DISCRIMINATOR);
}

export type UpdateListInstruction<
  TProgram extends string = typeof CANON_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountList extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountList extends string
        ? WritableAccount<TAccountList>
        : TAccountList,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateListInstructionData = {
  discriminator: ReadonlyUint8Array;
  submitDeposit: bigint;
  challengePct: number;
  listingWindow: bigint;
  withdrawalTimelock: bigint;
  newAuthority: Address;
};

export type UpdateListInstructionDataArgs = {
  submitDeposit: number | bigint;
  challengePct: number;
  listingWindow: number | bigint;
  withdrawalTimelock: number | bigint;
  newAuthority: Address;
};

export function getUpdateListInstructionDataEncoder(): FixedSizeEncoder<UpdateListInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["submitDeposit", getU64Encoder()],
      ["challengePct", getU16Encoder()],
      ["listingWindow", getU64Encoder()],
      ["withdrawalTimelock", getU64Encoder()],
      ["newAuthority", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_LIST_DISCRIMINATOR }),
  );
}

export function getUpdateListInstructionDataDecoder(): FixedSizeDecoder<UpdateListInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["submitDeposit", getU64Decoder()],
    ["challengePct", getU16Decoder()],
    ["listingWindow", getU64Decoder()],
    ["withdrawalTimelock", getU64Decoder()],
    ["newAuthority", getAddressDecoder()],
  ]);
}

export function getUpdateListInstructionDataCodec(): FixedSizeCodec<
  UpdateListInstructionDataArgs,
  UpdateListInstructionData
> {
  return combineCodec(
    getUpdateListInstructionDataEncoder(),
    getUpdateListInstructionDataDecoder(),
  );
}

export type UpdateListInput<
  TAccountAuthority extends string = string,
  TAccountList extends string = string,
> = {
  /**
   * Must equal `list.authority` (the creator at creation; rotatable via
   * this very instruction).
   */
  authority: TransactionSigner<TAccountAuthority>;
  list: Address<TAccountList>;
  submitDeposit: UpdateListInstructionDataArgs["submitDeposit"];
  challengePct: UpdateListInstructionDataArgs["challengePct"];
  listingWindow: UpdateListInstructionDataArgs["listingWindow"];
  withdrawalTimelock: UpdateListInstructionDataArgs["withdrawalTimelock"];
  newAuthority: UpdateListInstructionDataArgs["newAuthority"];
};

export function getUpdateListInstruction<
  TAccountAuthority extends string,
  TAccountList extends string,
  TProgramAddress extends Address = typeof CANON_PROGRAM_ADDRESS,
>(
  input: UpdateListInput<TAccountAuthority, TAccountList>,
  config?: { programAddress?: TProgramAddress },
): UpdateListInstruction<TProgramAddress, TAccountAuthority, TAccountList> {
  // Program address.
  const programAddress = config?.programAddress ?? CANON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    list: { value: input.list ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("list", accounts.list),
    ],
    data: getUpdateListInstructionDataEncoder().encode(
      args as UpdateListInstructionDataArgs,
    ),
    programAddress,
  } as UpdateListInstruction<TProgramAddress, TAccountAuthority, TAccountList>);
}

export type ParsedUpdateListInstruction<
  TProgram extends string = typeof CANON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Must equal `list.authority` (the creator at creation; rotatable via
     * this very instruction).
     */
    authority: TAccountMetas[0];
    list: TAccountMetas[1];
  };
  data: UpdateListInstructionData;
};

export function parseUpdateListInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateListInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), list: getNextAccount() },
    data: getUpdateListInstructionDataDecoder().decode(instruction.data),
  };
}
