/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { CANON_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_ITEM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  232, 80, 56, 56, 194, 171, 176, 235,
]);

export function getCloseItemDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_ITEM_DISCRIMINATOR);
}

export type CloseItemInstruction<
  TProgram extends string = typeof CANON_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountItem extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountItem extends string
        ? WritableAccount<TAccountItem>
        : TAccountItem,
      ...TRemainingAccounts,
    ]
  >;

export type CloseItemInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseItemInstructionDataArgs = {};

export function getCloseItemInstructionDataEncoder(): FixedSizeEncoder<CloseItemInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_ITEM_DISCRIMINATOR }),
  );
}

export function getCloseItemInstructionDataDecoder(): FixedSizeDecoder<CloseItemInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseItemInstructionDataCodec(): FixedSizeCodec<
  CloseItemInstructionDataArgs,
  CloseItemInstructionData
> {
  return combineCodec(
    getCloseItemInstructionDataEncoder(),
    getCloseItemInstructionDataDecoder(),
  );
}

export type CloseItemInput<
  TAccountCaller extends string = string,
  TAccountItem extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  item: Address<TAccountItem>;
};

export function getCloseItemInstruction<
  TAccountCaller extends string,
  TAccountItem extends string,
  TProgramAddress extends Address = typeof CANON_PROGRAM_ADDRESS,
>(
  input: CloseItemInput<TAccountCaller, TAccountItem>,
  config?: { programAddress?: TProgramAddress },
): CloseItemInstruction<TProgramAddress, TAccountCaller, TAccountItem> {
  // Program address.
  const programAddress = config?.programAddress ?? CANON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    item: { value: input.item ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("item", accounts.item),
    ],
    data: getCloseItemInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseItemInstruction<TProgramAddress, TAccountCaller, TAccountItem>);
}

export type ParsedCloseItemInstruction<
  TProgram extends string = typeof CANON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    caller: TAccountMetas[0];
    item: TAccountMetas[1];
  };
  data: CloseItemInstructionData;
};

export function parseCloseItemInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseItemInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { caller: getNextAccount(), item: getNextAccount() },
    data: getCloseItemInstructionDataDecoder().decode(instruction.data),
  };
}
