/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { CANON_PROGRAM_ADDRESS } from "../programs";

export const ADVANCE_PENDING_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [45, 2, 33, 230, 32, 255, 91, 161],
);

export function getAdvancePendingDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADVANCE_PENDING_DISCRIMINATOR,
  );
}

export type AdvancePendingInstruction<
  TProgram extends string = typeof CANON_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountList extends string | AccountMeta<string> = string,
  TAccountItem extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountList extends string
        ? ReadonlyAccount<TAccountList>
        : TAccountList,
      TAccountItem extends string
        ? WritableAccount<TAccountItem>
        : TAccountItem,
      ...TRemainingAccounts,
    ]
  >;

export type AdvancePendingInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AdvancePendingInstructionDataArgs = {};

export function getAdvancePendingInstructionDataEncoder(): FixedSizeEncoder<AdvancePendingInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ADVANCE_PENDING_DISCRIMINATOR }),
  );
}

export function getAdvancePendingInstructionDataDecoder(): FixedSizeDecoder<AdvancePendingInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAdvancePendingInstructionDataCodec(): FixedSizeCodec<
  AdvancePendingInstructionDataArgs,
  AdvancePendingInstructionData
> {
  return combineCodec(
    getAdvancePendingInstructionDataEncoder(),
    getAdvancePendingInstructionDataDecoder(),
  );
}

export type AdvancePendingInput<
  TAccountCaller extends string = string,
  TAccountList extends string = string,
  TAccountItem extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  list: Address<TAccountList>;
  item: Address<TAccountItem>;
};

export function getAdvancePendingInstruction<
  TAccountCaller extends string,
  TAccountList extends string,
  TAccountItem extends string,
  TProgramAddress extends Address = typeof CANON_PROGRAM_ADDRESS,
>(
  input: AdvancePendingInput<TAccountCaller, TAccountList, TAccountItem>,
  config?: { programAddress?: TProgramAddress },
): AdvancePendingInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountList,
  TAccountItem
> {
  // Program address.
  const programAddress = config?.programAddress ?? CANON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    list: { value: input.list ?? null, isWritable: false },
    item: { value: input.item ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("list", accounts.list),
      getAccountMeta("item", accounts.item),
    ],
    data: getAdvancePendingInstructionDataEncoder().encode({}),
    programAddress,
  } as AdvancePendingInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountList,
    TAccountItem
  >);
}

export type ParsedAdvancePendingInstruction<
  TProgram extends string = typeof CANON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    caller: TAccountMetas[0];
    list: TAccountMetas[1];
    item: TAccountMetas[2];
  };
  data: AdvancePendingInstructionData;
};

export function parseAdvancePendingInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdvancePendingInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      list: getNextAccount(),
      item: getNextAccount(),
    },
    data: getAdvancePendingInstructionDataDecoder().decode(instruction.data),
  };
}
