/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { CANON_PROGRAM_ADDRESS } from "../programs";
import {
  getUpdatePayloadDecoder,
  getUpdatePayloadEncoder,
  type UpdatePayload,
  type UpdatePayloadArgs,
} from "../types";

export const PROPOSE_COURT_UPDATE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([49, 107, 137, 118, 251, 154, 60, 31]);

export function getProposeCourtUpdateDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_COURT_UPDATE_DISCRIMINATOR,
  );
}

export type ProposeCourtUpdateInstruction<
  TProgram extends string = typeof CANON_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountList extends string | AccountMeta<string> = string,
  TAccountSubaccord extends string | AccountMeta<string> = string,
  TAccountPendingUpdate extends string | AccountMeta<string> = string,
  TAccountAccordProgram extends string | AccountMeta<string> =
    "cordhVoshqRV6kzGBmM89A66wuusJGsDCvLMHPLyKed",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountList extends string
        ? ReadonlyAccount<TAccountList>
        : TAccountList,
      TAccountSubaccord extends string
        ? ReadonlyAccount<TAccountSubaccord>
        : TAccountSubaccord,
      TAccountPendingUpdate extends string
        ? WritableAccount<TAccountPendingUpdate>
        : TAccountPendingUpdate,
      TAccountAccordProgram extends string
        ? ReadonlyAccount<TAccountAccordProgram>
        : TAccountAccordProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeCourtUpdateInstructionData = {
  discriminator: ReadonlyUint8Array;
  nonce: bigint;
  payload: UpdatePayload;
};

export type ProposeCourtUpdateInstructionDataArgs = {
  nonce: number | bigint;
  payload: UpdatePayloadArgs;
};

export function getProposeCourtUpdateInstructionDataEncoder(): Encoder<ProposeCourtUpdateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nonce", getU64Encoder()],
      ["payload", getUpdatePayloadEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PROPOSE_COURT_UPDATE_DISCRIMINATOR,
    }),
  );
}

export function getProposeCourtUpdateInstructionDataDecoder(): Decoder<ProposeCourtUpdateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nonce", getU64Decoder()],
    ["payload", getUpdatePayloadDecoder()],
  ]);
}

export function getProposeCourtUpdateInstructionDataCodec(): Codec<
  ProposeCourtUpdateInstructionDataArgs,
  ProposeCourtUpdateInstructionData
> {
  return combineCodec(
    getProposeCourtUpdateInstructionDataEncoder(),
    getProposeCourtUpdateInstructionDataDecoder(),
  );
}

export type ProposeCourtUpdateAsyncInput<
  TAccountCaller extends string = string,
  TAccountList extends string = string,
  TAccountSubaccord extends string = string,
  TAccountPendingUpdate extends string = string,
  TAccountAccordProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Must equal `list.authority`; pays the `PendingUpdate` rent — the list
   * PDA is data-carrying, so the system program rejects transfers from it
   * (same split as `create_dispute`, ADR-0028).
   */
  caller: TransactionSigner<TAccountCaller>;
  list: Address<TAccountList>;
  /**
   * The backing Accord Subaccord. Seeds link it to this list (`creator`,
   * `rules_hash`); `Account<Subaccord>` validates ownership, and the
   * explicit constraint pins the 1:1 pairing.
   */
  subaccord: Address<TAccountSubaccord>;
  /** The Accord `PendingUpdate` PDA — inited by the CPI with caller rent. */
  pendingUpdate?: Address<TAccountPendingUpdate>;
  /** Accord program (CPI target). */
  accordProgram?: Address<TAccountAccordProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  nonce: ProposeCourtUpdateInstructionDataArgs["nonce"];
  payload: ProposeCourtUpdateInstructionDataArgs["payload"];
};

export async function getProposeCourtUpdateInstructionAsync<
  TAccountCaller extends string,
  TAccountList extends string,
  TAccountSubaccord extends string,
  TAccountPendingUpdate extends string,
  TAccountAccordProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CANON_PROGRAM_ADDRESS,
>(
  input: ProposeCourtUpdateAsyncInput<
    TAccountCaller,
    TAccountList,
    TAccountSubaccord,
    TAccountPendingUpdate,
    TAccountAccordProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProposeCourtUpdateInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountList,
    TAccountSubaccord,
    TAccountPendingUpdate,
    TAccountAccordProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CANON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    list: { value: input.list ?? null, isWritable: false },
    subaccord: { value: input.subaccord ?? null, isWritable: false },
    pendingUpdate: { value: input.pendingUpdate ?? null, isWritable: true },
    accordProgram: { value: input.accordProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.pendingUpdate.value) {
    accounts.pendingUpdate.value = await getProgramDerivedAddress({
      programAddress:
        "cordhVoshqRV6kzGBmM89A66wuusJGsDCvLMHPLyKed" as Address<"cordhVoshqRV6kzGBmM89A66wuusJGsDCvLMHPLyKed">,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([117, 112, 100, 97, 116, 101])),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "subaccord",
            accounts.subaccord.value,
          ),
        ),
        getU64Encoder().encode(
          getNonNullResolvedInstructionInput("nonce", args.nonce),
        ),
      ],
    });
  }
  if (!accounts.accordProgram.value) {
    accounts.accordProgram.value =
      "cordhVoshqRV6kzGBmM89A66wuusJGsDCvLMHPLyKed" as Address<"cordhVoshqRV6kzGBmM89A66wuusJGsDCvLMHPLyKed">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("list", accounts.list),
      getAccountMeta("subaccord", accounts.subaccord),
      getAccountMeta("pendingUpdate", accounts.pendingUpdate),
      getAccountMeta("accordProgram", accounts.accordProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getProposeCourtUpdateInstructionDataEncoder().encode(
      args as ProposeCourtUpdateInstructionDataArgs,
    ),
    programAddress,
  } as ProposeCourtUpdateInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountList,
    TAccountSubaccord,
    TAccountPendingUpdate,
    TAccountAccordProgram,
    TAccountSystemProgram
  >);
}

export type ProposeCourtUpdateInput<
  TAccountCaller extends string = string,
  TAccountList extends string = string,
  TAccountSubaccord extends string = string,
  TAccountPendingUpdate extends string = string,
  TAccountAccordProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Must equal `list.authority`; pays the `PendingUpdate` rent — the list
   * PDA is data-carrying, so the system program rejects transfers from it
   * (same split as `create_dispute`, ADR-0028).
   */
  caller: TransactionSigner<TAccountCaller>;
  list: Address<TAccountList>;
  /**
   * The backing Accord Subaccord. Seeds link it to this list (`creator`,
   * `rules_hash`); `Account<Subaccord>` validates ownership, and the
   * explicit constraint pins the 1:1 pairing.
   */
  subaccord: Address<TAccountSubaccord>;
  /** The Accord `PendingUpdate` PDA — inited by the CPI with caller rent. */
  pendingUpdate: Address<TAccountPendingUpdate>;
  /** Accord program (CPI target). */
  accordProgram?: Address<TAccountAccordProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  nonce: ProposeCourtUpdateInstructionDataArgs["nonce"];
  payload: ProposeCourtUpdateInstructionDataArgs["payload"];
};

export function getProposeCourtUpdateInstruction<
  TAccountCaller extends string,
  TAccountList extends string,
  TAccountSubaccord extends string,
  TAccountPendingUpdate extends string,
  TAccountAccordProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CANON_PROGRAM_ADDRESS,
>(
  input: ProposeCourtUpdateInput<
    TAccountCaller,
    TAccountList,
    TAccountSubaccord,
    TAccountPendingUpdate,
    TAccountAccordProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ProposeCourtUpdateInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountList,
  TAccountSubaccord,
  TAccountPendingUpdate,
  TAccountAccordProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CANON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    list: { value: input.list ?? null, isWritable: false },
    subaccord: { value: input.subaccord ?? null, isWritable: false },
    pendingUpdate: { value: input.pendingUpdate ?? null, isWritable: true },
    accordProgram: { value: input.accordProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.accordProgram.value) {
    accounts.accordProgram.value =
      "cordhVoshqRV6kzGBmM89A66wuusJGsDCvLMHPLyKed" as Address<"cordhVoshqRV6kzGBmM89A66wuusJGsDCvLMHPLyKed">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("list", accounts.list),
      getAccountMeta("subaccord", accounts.subaccord),
      getAccountMeta("pendingUpdate", accounts.pendingUpdate),
      getAccountMeta("accordProgram", accounts.accordProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getProposeCourtUpdateInstructionDataEncoder().encode(
      args as ProposeCourtUpdateInstructionDataArgs,
    ),
    programAddress,
  } as ProposeCourtUpdateInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountList,
    TAccountSubaccord,
    TAccountPendingUpdate,
    TAccountAccordProgram,
    TAccountSystemProgram
  >);
}

export type ParsedProposeCourtUpdateInstruction<
  TProgram extends string = typeof CANON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Must equal `list.authority`; pays the `PendingUpdate` rent — the list
     * PDA is data-carrying, so the system program rejects transfers from it
     * (same split as `create_dispute`, ADR-0028).
     */
    caller: TAccountMetas[0];
    list: TAccountMetas[1];
    /**
     * The backing Accord Subaccord. Seeds link it to this list (`creator`,
     * `rules_hash`); `Account<Subaccord>` validates ownership, and the
     * explicit constraint pins the 1:1 pairing.
     */
    subaccord: TAccountMetas[2];
    /** The Accord `PendingUpdate` PDA — inited by the CPI with caller rent. */
    pendingUpdate: TAccountMetas[3];
    /** Accord program (CPI target). */
    accordProgram: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: ProposeCourtUpdateInstructionData;
};

export function parseProposeCourtUpdateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeCourtUpdateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      list: getNextAccount(),
      subaccord: getNextAccount(),
      pendingUpdate: getNextAccount(),
      accordProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getProposeCourtUpdateInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
