/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { CANON_PROGRAM_ADDRESS } from "../programs";

export const SETTLE_ITEM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  27, 140, 204, 32, 73, 91, 135, 203,
]);

export function getSettleItemDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SETTLE_ITEM_DISCRIMINATOR);
}

export type SettleItemInstruction<
  TProgram extends string = typeof CANON_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountList extends string | AccountMeta<string> = string,
  TAccountItem extends string | AccountMeta<string> = string,
  TAccountDispute extends string | AccountMeta<string> = string,
  TAccountFeeMint extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountChallengerTokenAccount extends string | AccountMeta<string> = string,
  TAccountSubmitterTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountList extends string
        ? WritableAccount<TAccountList>
        : TAccountList,
      TAccountItem extends string
        ? WritableAccount<TAccountItem>
        : TAccountItem,
      TAccountDispute extends string
        ? ReadonlyAccount<TAccountDispute>
        : TAccountDispute,
      TAccountFeeMint extends string
        ? ReadonlyAccount<TAccountFeeMint>
        : TAccountFeeMint,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountChallengerTokenAccount extends string
        ? WritableAccount<TAccountChallengerTokenAccount>
        : TAccountChallengerTokenAccount,
      TAccountSubmitterTokenAccount extends string
        ? WritableAccount<TAccountSubmitterTokenAccount>
        : TAccountSubmitterTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SettleItemInstructionData = { discriminator: ReadonlyUint8Array };

export type SettleItemInstructionDataArgs = {};

export function getSettleItemInstructionDataEncoder(): FixedSizeEncoder<SettleItemInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SETTLE_ITEM_DISCRIMINATOR }),
  );
}

export function getSettleItemInstructionDataDecoder(): FixedSizeDecoder<SettleItemInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSettleItemInstructionDataCodec(): FixedSizeCodec<
  SettleItemInstructionDataArgs,
  SettleItemInstructionData
> {
  return combineCodec(
    getSettleItemInstructionDataEncoder(),
    getSettleItemInstructionDataDecoder(),
  );
}

export type SettleItemAsyncInput<
  TAccountCaller extends string = string,
  TAccountList extends string = string,
  TAccountItem extends string = string,
  TAccountDispute extends string = string,
  TAccountFeeMint extends string = string,
  TAccountVault extends string = string,
  TAccountChallengerTokenAccount extends string = string,
  TAccountSubmitterTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  list: Address<TAccountList>;
  item: Address<TAccountItem>;
  /**
   * Accord Dispute — `Account<Dispute>` validates ownership + deserialises;
   * `state` + `final_ruling` are read directly.
   */
  dispute: Address<TAccountDispute>;
  feeMint: Address<TAccountFeeMint>;
  vault?: Address<TAccountVault>;
  /**
   * Challenger's `fee_mint` token account — receives the bounty on `remove`
   * and the stake refund on a `Failed` dispute. Pinned to the payee recorded
   * on the item so a crank caller cannot redirect the payout (C-1).
   */
  challengerTokenAccount: Address<TAccountChallengerTokenAccount>;
  /**
   * Submitter's `fee_mint` token account — receives the payout on
   * withdrawal-`keep` and the refund on a `Failed` dispute. Pinned the same
   * way (C-1).
   */
  submitterTokenAccount: Address<TAccountSubmitterTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export async function getSettleItemInstructionAsync<
  TAccountCaller extends string,
  TAccountList extends string,
  TAccountItem extends string,
  TAccountDispute extends string,
  TAccountFeeMint extends string,
  TAccountVault extends string,
  TAccountChallengerTokenAccount extends string,
  TAccountSubmitterTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof CANON_PROGRAM_ADDRESS,
>(
  input: SettleItemAsyncInput<
    TAccountCaller,
    TAccountList,
    TAccountItem,
    TAccountDispute,
    TAccountFeeMint,
    TAccountVault,
    TAccountChallengerTokenAccount,
    TAccountSubmitterTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SettleItemInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountList,
    TAccountItem,
    TAccountDispute,
    TAccountFeeMint,
    TAccountVault,
    TAccountChallengerTokenAccount,
    TAccountSubmitterTokenAccount,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CANON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    list: { value: input.list ?? null, isWritable: true },
    item: { value: input.item ?? null, isWritable: true },
    dispute: { value: input.dispute ?? null, isWritable: false },
    feeMint: { value: input.feeMint ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    challengerTokenAccount: {
      value: input.challengerTokenAccount ?? null,
      isWritable: true,
    },
    submitterTokenAccount: {
      value: input.submitterTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress:
        "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">,
      seeds: [
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("list", accounts.list.value),
        ),
        getBytesEncoder().encode(
          new Uint8Array([
            6, 221, 246, 225, 215, 101, 161, 147, 217, 203, 225, 70, 206, 235,
            121, 172, 28, 180, 133, 237, 95, 91, 55, 145, 58, 140, 245, 133,
            126, 255, 0, 169,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "feeMint",
            accounts.feeMint.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("list", accounts.list),
      getAccountMeta("item", accounts.item),
      getAccountMeta("dispute", accounts.dispute),
      getAccountMeta("feeMint", accounts.feeMint),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("challengerTokenAccount", accounts.challengerTokenAccount),
      getAccountMeta("submitterTokenAccount", accounts.submitterTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getSettleItemInstructionDataEncoder().encode({}),
    programAddress,
  } as SettleItemInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountList,
    TAccountItem,
    TAccountDispute,
    TAccountFeeMint,
    TAccountVault,
    TAccountChallengerTokenAccount,
    TAccountSubmitterTokenAccount,
    TAccountTokenProgram
  >);
}

export type SettleItemInput<
  TAccountCaller extends string = string,
  TAccountList extends string = string,
  TAccountItem extends string = string,
  TAccountDispute extends string = string,
  TAccountFeeMint extends string = string,
  TAccountVault extends string = string,
  TAccountChallengerTokenAccount extends string = string,
  TAccountSubmitterTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  list: Address<TAccountList>;
  item: Address<TAccountItem>;
  /**
   * Accord Dispute — `Account<Dispute>` validates ownership + deserialises;
   * `state` + `final_ruling` are read directly.
   */
  dispute: Address<TAccountDispute>;
  feeMint: Address<TAccountFeeMint>;
  vault: Address<TAccountVault>;
  /**
   * Challenger's `fee_mint` token account — receives the bounty on `remove`
   * and the stake refund on a `Failed` dispute. Pinned to the payee recorded
   * on the item so a crank caller cannot redirect the payout (C-1).
   */
  challengerTokenAccount: Address<TAccountChallengerTokenAccount>;
  /**
   * Submitter's `fee_mint` token account — receives the payout on
   * withdrawal-`keep` and the refund on a `Failed` dispute. Pinned the same
   * way (C-1).
   */
  submitterTokenAccount: Address<TAccountSubmitterTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getSettleItemInstruction<
  TAccountCaller extends string,
  TAccountList extends string,
  TAccountItem extends string,
  TAccountDispute extends string,
  TAccountFeeMint extends string,
  TAccountVault extends string,
  TAccountChallengerTokenAccount extends string,
  TAccountSubmitterTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof CANON_PROGRAM_ADDRESS,
>(
  input: SettleItemInput<
    TAccountCaller,
    TAccountList,
    TAccountItem,
    TAccountDispute,
    TAccountFeeMint,
    TAccountVault,
    TAccountChallengerTokenAccount,
    TAccountSubmitterTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SettleItemInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountList,
  TAccountItem,
  TAccountDispute,
  TAccountFeeMint,
  TAccountVault,
  TAccountChallengerTokenAccount,
  TAccountSubmitterTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CANON_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    list: { value: input.list ?? null, isWritable: true },
    item: { value: input.item ?? null, isWritable: true },
    dispute: { value: input.dispute ?? null, isWritable: false },
    feeMint: { value: input.feeMint ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    challengerTokenAccount: {
      value: input.challengerTokenAccount ?? null,
      isWritable: true,
    },
    submitterTokenAccount: {
      value: input.submitterTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("list", accounts.list),
      getAccountMeta("item", accounts.item),
      getAccountMeta("dispute", accounts.dispute),
      getAccountMeta("feeMint", accounts.feeMint),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("challengerTokenAccount", accounts.challengerTokenAccount),
      getAccountMeta("submitterTokenAccount", accounts.submitterTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getSettleItemInstructionDataEncoder().encode({}),
    programAddress,
  } as SettleItemInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountList,
    TAccountItem,
    TAccountDispute,
    TAccountFeeMint,
    TAccountVault,
    TAccountChallengerTokenAccount,
    TAccountSubmitterTokenAccount,
    TAccountTokenProgram
  >);
}

export type ParsedSettleItemInstruction<
  TProgram extends string = typeof CANON_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    caller: TAccountMetas[0];
    list: TAccountMetas[1];
    item: TAccountMetas[2];
    /**
     * Accord Dispute — `Account<Dispute>` validates ownership + deserialises;
     * `state` + `final_ruling` are read directly.
     */
    dispute: TAccountMetas[3];
    feeMint: TAccountMetas[4];
    vault: TAccountMetas[5];
    /**
     * Challenger's `fee_mint` token account — receives the bounty on `remove`
     * and the stake refund on a `Failed` dispute. Pinned to the payee recorded
     * on the item so a crank caller cannot redirect the payout (C-1).
     */
    challengerTokenAccount: TAccountMetas[6];
    /**
     * Submitter's `fee_mint` token account — receives the payout on
     * withdrawal-`keep` and the refund on a `Failed` dispute. Pinned the same
     * way (C-1).
     */
    submitterTokenAccount: TAccountMetas[7];
    tokenProgram: TAccountMetas[8];
  };
  data: SettleItemInstructionData;
};

export function parseSettleItemInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSettleItemInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 9,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      list: getNextAccount(),
      item: getNextAccount(),
      dispute: getNextAccount(),
      feeMint: getNextAccount(),
      vault: getNextAccount(),
      challengerTokenAccount: getNextAccount(),
      submitterTokenAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getSettleItemInstructionDataDecoder().decode(instruction.data),
  };
}
